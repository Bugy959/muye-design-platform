# 木叶义齿·设计平台 交接文档

**V2.9 | 更新于 2026年8月21日**

## 一、项目概况

义齿数字化设计协作 SPA，React 19 + TypeScript + Vite 7。三端：医院端 / 设计师端 / 管理端。

V2.0 需求清单（见 `V2.0-需求待办与确认清单.md`）24 项任务已全部实现，2026-08-04 完成接线修复，`npm run build` 通过（0 编译错误）。

## 二、环境与启动

- Node.js ≥ 18（本机已验证 v24）。首次运行：`cd website && npm install`
- 开发模式：双击 `start.bat`（或 `cd website && npm run dev`）→ http://localhost:3000
- 预览打包版：双击 `open.bat`（无 dist 时自动先构建）→ http://localhost:8080
- 手动构建：`cd website && npm run build`

start.bat / open.bat 已于 8/4 重写：

- 自动兜底 npm 路径（`C:\Program Files\nodejs`）
- 启动前自动释放被占用的 3000 / 8080 端口
- 浏览器延迟 5 秒打开，避免"服务器未就绪就弹页面"
- 内容纯英文，避免 bat 中文乱码报错
- 8/21 起 start.bat 同时拉起后端（:3001）与前端（:3000），打开页面即可连接后端

局域网访问：另一台电脑浏览器输入 `http://<本机IP>:3000`（开发模式）或 `http://<本机IP>:8080`（预览模式，需先启动后端），当前本机 IP 192.168.1.5（IP 变化请以 `ipconfig` 为准）；需同一网络，Windows 防火墙已放行 Node.js。2026-08-21 起前端自动适配：页面里的 API 地址跟随浏览器当前主机名拼成 `http://<本机IP>:3001/api`，局域网设备无需改任何配置（`VITE_API_BASE` 仍可覆盖，用于生产 nginx 反代 `/api`）。

## 三、核心文件（website/src/ 目录）

| 文件 | 职责 |
|---|---|
| types/index.ts | 数据模型：FDI 牙位编码、基台类型、返工申请、设计参数 |
| lib/store.ts | 全部业务逻辑（>30KB）：订单/积分/分组匹配/返工审核/搜索/统计/旧数据迁移 |
| sections/AdminApp.tsx | 管理端：概览/账号/积分(含设计参数)/订单(搜索+筛选+导出)/账单(含基台列)/分组/匹配/返工审核 |
| sections/ClientApp.tsx | 医院端：设计师设计(下单)/AI设计/用户服务(退回+返工)/积分明细/消息/我的订单(搜索) |
| sections/DesignerApp.tsx | 设计师端：接单大厅(匹配过滤+参数横条+统计卡片)/我的订单 |
| sections/Login.tsx | 登录页（logo 主题封面） |
| components/bits.tsx | 通用组件：徽章/表格头/FileChip/ImageThumb(照片放大+下载)/OrderScope |
| components/ToothChart.tsx | FDI 牙位选择图（上下颌双行，11-48 双位数） |
| lib/api.ts | 统一 API client（V2.9 新增）：31 个接口封装、Bearer token、统一错误处理 |
| lib/order-utils.ts / use-now.ts | 排序工具与当前时间 hook（V2.8 迁出/新增） |

## 四、V2.0 关键变更（24 项全部完成）

| 模块 | 说明 |
|---|---|
| 牙位编码 | FDI 双位数（11-18 右上 / 21-28 左上 / 31-38 左下 / 41-48 右下；每侧 8 颗含智齿；图上左半 上1x/下4x、右半 上2x/下3x），旧编码自动迁移 |
| 设计类型 | 新增「基台」5 分/颗；即刻设计=加急件标注；马龙桥 80 分/件（上颌/下颌/全口） |
| 文案统一 | 「口扫文件」全部改为「扫描文件」（不限格式） |
| 医院端导航 | 设计师设计 → AI 设计 → 用户服务 → 积分明细 → 消息 → 我的订单（最右） |
| 设计师端 | 卡片取消折叠详情常显；设计参数彩色横条（内冠间隙/咬合切/邻接切）；4 格统计卡片；返工附图可见 |
| 管理端 | 客户名旁配置设计参数；订单搜索+状态筛选+导出 CSV；账单含基台列；分组备注深色另起一行 |
| 隐私保护 | 设计师端不显示医院名称/电话；账单与导出仅显示"X 师傅" |
| 照片/文件 | 三端照片点击放大（Lightbox）+ 独立下载；文件下载与照片下载分开 |
| 返工流程 | 客户端申请（留言+照片）→ 订单直接回原设计师重做；管理端审核仅登记+决定是否退积分（V2.3 起，2026-08-08） |
| AI 设计 | 占位页（功能正在开发，敬请期待） |

## 五、8月4日修复记录（重要）

接手时项目处于编译失败状态（18 个 TypeScript 错误，V2.0 改到一半），本次全部修复并补完半成品功能：

1. ToothChart 重写为 FDI 编码；ensureDBShape 增加旧牙位编码（如 UL3）→ FDI 自动迁移，浏览器旧订单数据不丢。
2. ClientApp 导航接线：修复 tab 类型与未声明变量，删除残留的 DesignService 组件与重复 AI 占位块；OrderList 增加单号+日期搜索。
3. DesignerApp：修复 PoolCard 残留 setOpen 报错；详情常显；补设计参数横条与统计卡片；返工申请附图在卡片中展示。
4. AdminApp：补写 ParamEditor 组件；BillRow 加 jita 基台列（表格+CSV 同步）；订单总览加搜索框与导出 CSV 按钮。
5. bits.tsx 新增共用 ImageThumb；三端照片展示统一替换。

## 六、8月4日 UI 调整记录（16:20）

1. FDI 图上排布按老板确认调整：左半 上 1x / 下 4x，右半 上 2x / 下 3x；toothLabel 修正为 31-38=左下、41-48=右下（原标注左右相反）。
2. 牙位图格子缩小（w-9→w-7 等），修复在设计师端半宽卡片中右侧 16/17/36/37 被裁切的溢出问题。
3. 牙位图底部 "FDI 牙位 · …" 图例行删除，仅保留"已选 N 颗"。
4. 全站英文标识清理：Hospital / Lab / Designer · / Admin · / Submitted / Muye Dental Design Platform 等全部移除，界面仅保留中文。
5. ImageThumb 无图内容时文件名自动折行，修复缩略图溢出。
6. V2.1 第 8 项落地（21:40）：订单新增「患者姓名/编号」选填字段（设计师可见患者名，不可见医院信息）；医院端订单行显示该患者历史订单；管理端订单总览点击客户名可筛选、CSV 导出加患者列；订单搜索支持患者姓名。
7. V2.1 第 6 项落地（21:50）：恢复医院端「消息」页（原代码被 `{false && ...}` 禁用，点进去空白）；返工审核通过/不通过现在也会给客户推送消息提醒（订单完成、退回提醒此前已有）。
8. V2.1 第 2/3/9 项落地（21:55）：设计师接单成功后自动跳转「我的订单」（acceptOrder 现在返回是否抢单成功）；管理端返工审核通过/驳回后弹出 Toast 提示（sonner，main.tsx 已挂载 Toaster）；设计师接单大厅顶部显示"手上有 N 单进行中"。
9. V2.1 第 13/10 项落地（22:00）：返工申请在管理端审核前，客户可在「用户服务」修改（留言+重传照片）或撤销（订单恢复已完成）；管理端超过 24 小时未接的待接订单在订单总览标红"超时未接"、可一键筛选，订单概览页新增超时统计格。
10. V2.1 第 7/15 项落地（22:10）：医院端订单卡片新增流程时间线（提交订单→设计师接单→设计完成，退回/返工审核中为红色终态节点，组件 OrderTimeline 在 bits.tsx）；订单新增 returnedAt 字段（退回时记录时间）；管理端订单展开详情底部新增「操作时间线」，按时间列出提交/接单/完成/退回/返工申请/审核结果等全部事件。
11. V2.1 第 4 项落地（22:20）：医院端订单可撤回——只要还没有设计师接单（待接单/未分配状态），订单卡片右上角显示「撤回订单」，确认后预扣积分全额退回并记积分流水，订单状态变为「已撤回」（新增 cancelled 状态 + cancelledAt 字段），立即从接单大厅消失；流程时间线和管理端操作时间线均显示撤回节点；管理端订单总览可按「已撤回」筛选。
12. 服务器化整改启动（2026-08-05）：新建 `server/` 后端骨架——Node 24 + Express + 内置 SQLite（node:sqlite），13 张表（含设计参数表）对应前端数据模型；密码 scrypt 加密、登录令牌 7 天、积分事务防超扣、抢单原子锁、角色数据隔离（医院只见自己/设计师不见医院与积分/管理全量）；全部业务 API 与 store.ts 规则一一对应（下单预扣/抢单/交稿/退回/重提多退少补/撤回退分/返工审核/分组匹配/账号管理/设计参数）。烟测全流程通过（下单→抢单锁→交稿→撤回→充值→流水核对）。部署路线与清单见根目录《服务器部署整改方案.md》。
13. V2.1 清单收口（2026-08-05）：完成最后 5 项——第 5 项订单列表排序（医院端与管理端均加「最新/最早/积分高/按状态」切换，组件 SortBar/sortOrders 在 bits.tsx）；第 11 项上传文件显示大小（OrderFile/OrderImage 新增 size 字段，扫描文件显示大小、照片缩略图底部大小角标）；第 12 项上传区颜色区分（扫描=蓝色系、照片=绿色系）；第 17 项进入下单页自动聚焦患者姓名输入框；第 18 项管理端六张概览卡片可点击，直达订单总览并自动套好状态筛选（含超时未接）。第 14/16/19/20 项经评估不实施（原因已写入清单），第 1 项「复用上次订单」待定。清单最终：15 完成 / 4 不实施 / 1 待定。
14. UI 融合方案实施（2026-08-06）：依据《UI升级三方案.md》三份规范折中落地，主色 #1e5c46 与暖纸背景 #fdfaf6 不变。① 三端导航由矩形色块改为底部 2px 品牌绿指示线（-mb-[2px] 对齐整体底线）；② 主/次按钮直角→4px 圆角并补足 44px 触控高度，输入框 4px 圆角 + 焦点环加粗到 3px；③ 状态徽章与加急件标签改全圆角；④ 全站分界线加深一档（表头 2px 线、行分隔 stone-200→300、卡片边框 stone-300）；⑤ 管理端五张表格加偶数行暖石淡底 + hover 行底色；⑥ 医院端订单列表行 hover 淡绿底色；⑦ 卡片统一 6px 圆角（概览卡片 hover 阴影升 md），弹窗原已是 8px+大阴影保持不变。评估后砍掉：卡片彩色左边线（状态已有徽章+时间线表达）、绿色投影、品牌色变更、背景点阵纹理、三套新字体。
15. V2.3 审计修复与返工规则调整（2026-08-08）：对照第三方审计 18 条逐条核实（全部属实）后处理——① **返工流程按老板最终规则重写（前后端同步）**：客户申请返工后订单立即回到原设计师手上重做（保留 designerId，不进接单大厅、不可被抢；接单大厅与抢单锁仅限 pending），管理端审核只做"登记+决定是否退积分"（通过退全额积分/不通过不退，订单均留原设计师）；设计师重新提交设计文件即完成且返工次数+1；审核前客户撤销申请则恢复已完成（有过返工记录保留返工标记）；删除绕过审核的死代码 requestRework；后端烟测全链路通过（下单→完成→申请→审核退分→原设计师重交）。② **积分防负**：管理端扣减超过余额时前后端均拦截并提示。③ 概览「返工」卡片数字与跳转统一为"所有返工过的订单"口径（订单总览新增「返工订单」筛选按钮）。④ 医院端订单行直接显示积分；订单表格与 CSV 的积分列不再对返工单显示 0。⑤ 圆角定稿落实：--radius 基准调为 8px（卡片 rounded-md=6px、弹窗 rounded-lg=8px），补齐管理端 4 处漏圆角面板。⑥ 时间线"返工审核中"改"返工重做中"；返工审核页文案同步新规则。⑦ 文档类对齐：管理端导航顺序、AI 占位文案、消息自动已读、参数横条颜色、撤回灰色节点、加急红色徽章、后端实为 13 张表等，全部以代码为准更新进文档。
16. V2.4 设计师端长条化改版（2026-08-13）：老板先在预览页（根目录 `UI预览-设计师端长条订单.html`，带像素标注）确认尺寸后实施——接单大厅 PoolCard 由半宽大卡片（约 470×420~560px，一屏 2~3 单）改为通栏长条（1024×120px，一屏 6~7 单）；新增 `ToothChartMini` 迷你牙位图组件（格子 14×14、十字分区线、四象限角标「右上/左上/右下/左下」、图下方直接列出选中编号，三重防数错）；马龙桥/自定义单牙位区显示范围徽章；长条点击任意处展开详情区（完整要求/返工信息/扫描文件/照片）；按老板要求新增「↓ 全部下载」按钮，一键依次下载该单全部扫描文件+照片（接单大厅展开区与「我的订单」均有；演示种子订单未保存文件内容时按钮置灰并提示）。同日「我的订单」MineRow 也改为同一长条语言：条内为单号/徽章+类型+迷你牙位图+要求摘要+状态，进行中的订单条右侧直接放「完成订单」按钮（点开即进入上传设计文件流程），展开区内含完整要求、返工要求与附图、扫描文件+全部下载、照片、上传/退回操作；已完成订单展开区显示已提交的设计文件。随后老板拍板「全部统一」：医院端「我的订单」同步改为通栏长条（单号+返工×N/加急徽章 → 类型 → 迷你牙位图 → 要求+患者/设计师/积分/时间 → 状态+撤回按钮），点击展开含患者历史订单、时间线、退回重提、照片、设计文件批量下载与申请返工表单；同时修正即刻设计单在长条上漏显示「加急件」徽章的问题（jike 类型恒为加急）。管理端订单总览保持表格形态（含排序/筛选/导出），为三端中唯一表格视图。
17. V2.5 牙位图统一 8 颗（2026-08-19）：按老板确认，牙位编码从每侧 7 颗扩展为每侧 8 颗（含智齿），上右 11-18 / 上左 21-28 / 下左 31-38 / 下右 41-48；医院端牙位选择图与设计师端迷你牙位图每排统一为 8 颗并保持十字分隔线；同时修复迷你牙位图循环 i<16 导致的 undefined 空白格，并清理重复的 ToothChartMini 组件。开发文档/交接文档同步更新为 V2.5。
18. V2.6 工程与体验优化（2026-08-19）：消息支持单条已读（后端补 `POST /api/notices/:id/read`）；Vite 代码分包（React/shadcn/业务三块）；新增 Vitest 核心纯函数测试（53 个）；新增 Git 版本管理 + .gitignore + GitHub Actions CI（tsc + build）。复用上次订单、订单详情抽屉、Ctrl+K 全局搜索、前后端类型共享标记为待后续迭代。
19. V2.7/V2.8 质量整改（2026-08-19）：新增《前端改造与质量清单.md》明确前端 P0-P3 改造项；lint 45 个问题清零（0 error / 0 warning）；删除 50 个未使用的 shadcn 模板组件（仅保留 alert-dialog/button/sonner）；`fmtSize`/`sortOrders` 迁至 `lib/order-utils.ts`、`buttonVariants` 迁至 `ui/button-variants.ts`；新增 `lib/use-now.ts`；CI 增加 lint 与覆盖率门槛（行覆盖 ≥ 90%，当前 98.94%）。
20. V2.9 前端对接后端 API（2026-08-21，P0 完成）：新增 `lib/api.ts` 统一 API client（31 个接口封装、Bearer token、统一错误处理）；store.ts 新增后端同步层（本地乐观更新 + 同步 API + `/api/bootstrap` 覆盖刷新，失败自动回滚为服务端数据）；登录/退出与三端全部业务操作对接后端；App 增加后端初始化（加载页/连接失败页可重试、可切演示数据）与 15 秒轮询；抢单失败提示服务端消息（如"手慢了"）；后端补单条消息已读接口（含越权测试）；顶栏「演示数据」按钮（演示模式下显示，点击切回后端）/ localStorage `muye-data-mode=demo` 切入演示模式；全端口牙位展示统一为 FDI 编号（如 21、23）；start.bat 改为同时拉起后端（:3001）与前端（:3000）。随后补后端同步层测试 18 个（tests/core-sync.test.js，mock window + fetch，覆盖乐观更新/失败回滚/刷新合并），覆盖率门槛通过（store.ts 96.37% / 全量 95.25%）；后端补「医院读他人订单」越权用例（bootstrap 数据隔离 + 跨医院撤回/返工被拒）。lint / build / 前端 71 测试（53 核心 + 18 后端同步层）/ 后端 14 测试 / 冒烟联调全绿。

## 七、演示账号

- 管理端：admin / muye2026
- 医院/加工厂：mingzhou / hengmei / yahe / 123456
- 设计师：li / wang / zhao / sun / zhou / 123456

## 八、UI 设计系统（2026-08-06 融合方案定稿）

| 项目 | 内容 |
|------|------|
| 设计基调 | 暖纸面 + 品牌绿（方案 A/B/C 折中，主色与背景不变） |
| 字体 | Figtree（统计数字标签）+ PingFang SC / Microsoft YaHei（中文正文） |
| 配色 | 暖白纸面 #fdfaf6 + 品牌绿 #1e5c46 + 琥珀强调 #D97706（待处理/超时）；加急件徽章为红色 #b3402f（更醒目） |
| 圆角层次 | 按钮/输入框 4px · 卡片 6px · 弹窗 8px · 徽章/标签全圆角 |
| 阴影层次 | 卡片 shadow-sm · hover shadow-md · 弹窗 shadow-lg（中性灰黑，不用彩色投影） |
| 导航 | 三端统一底部 2px 品牌绿指示线（激活项），非激活项无背景 |
| 表格 | 表头 2px 底线、偶数行暖石淡底、hover 行底色、行分隔 stone-200 |
| 分界线 | 全站 stone-300 一档加深（2026-08-06 老板反馈太淡） |
| 焦点 | 输入框 3px 品牌绿焦点环；全局 focus-visible 轮廓 |
| 触控 | 主按钮 min-height 44px；全局 cursor-pointer |
| 无障碍 | prefers-reduced-motion 动画偏好 |
| 明确不做 | 卡片彩色左边线、绿色投影、品牌色变更、背景纹理、额外字体族 |

## 九、数据说明

V2.9 起（2026-08-21）前端默认连接后端 API（`server/`，数据存 `data/muye.db`）。localStorage（key：`muye-design-platform-v5`）仅作演示模式与本地缓存：顶栏「演示数据」按钮或设置 localStorage `muye-data-mode=demo` 可切回纯本地演示模式；清除浏览器数据不影响后端数据。

## 十、V2.1 优化（20项）

2026-08-04 新增 UI/UX 优化清单，含交互动效/布局/质感/信息展示四类。

## 十一、上线待办

前端已对接后端 API（P0 完成，2026-08-21）。剩余：① ~~STL 大文件对象存储（OSS/COS）~~ ✅ 代码已实现（2026-08-23），待配置云账号密钥；② 购买服务器 + 域名 + ICP 备案 + HTTPS；③ 数据库每日自动备份；④ 患者影像保密协议。详见根目录《服务器部署整改方案.md》。

## 十二、路径与启动速查

- 项目路径：`E:\木叶设计平台\website`
- 后端路径：`E:\木叶设计平台\server`（`npm install` 后 `npm start`，:3001，详见 server/README.md 与《服务器部署整改方案.md》）
- 启动：`start.bat`（开发，同时拉起后端 :3001 与前端 :3000）/ `open.bat`（预览 :8080）
- 使用说明（2026-08-13 新增）：`使用说明-医院端.md` / `使用说明-设计师端.md` / `使用说明-管理端.md`


## 十三、2026-08-23 变更记录（OSS 大文件直传 + 测试完善）

1. **后端 OSS 直传**：新增 `server/src/files.js`（预签名上传/下载，单文件 1GB 上限），新增 `POST /api/files/upload-token`；bootstrap 自动把订单文件 key 转为签名下载地址；未配置 OSS 时安全降级、不影响其它接口。
2. **前端接入**：`readOrderFile` 大文件在后端模式自动走 OSS 直传（未配置 OSS 时回退旧逻辑）；订单图片/文件展示与批量下载支持 OSS 签名地址（`bits.tsx` / `ClientApp` / `DesignerApp`）。
3. **测试完善**：后端 23 个、前端 96 个测试全部通过；覆盖率门槛通过（前端全部文件 99.29% / store.ts 99.5%）。新增 OSS 直传接口与前端上传测试（本地签名，不联网）。
4. **依赖**：后端新增 `ali-oss`。
5. **待办**：开通 OSS/COS 私有桶 → 配 CORS → 服务器填环境变量 → 真机验证大文件全流程（上传→下单→下载）。

## 十四、文档速查（2026-08-23 新增）

- `服务器部署详细指南.md` —— 部署实操（第 12 章为大文件 OSS 方案）
- `测试说明与部署清单.md` —— 测试补充说明（小白版）+ 部署清单


## 十五、2026-08-24 变更记录（上传升级：分片直传 + 断点续传）

1. **上传提速与稳定**：大文件（>50MB）改为**分片直传**——后端新增 `POST /api/files/upload-init` / `upload-part-url` / `upload-complete` 三个接口；前端 `uploadOrderFile` 自动选择单次/分片，5MB/片、并发 4，**断网/刷新可断点续传**（完成分片写入 localStorage 检查点）。
2. **有效期调大**：上传预签名 10 分钟 → **1 小时**；下载 2 小时（`server/src/files.js`）。
3. **注意**：OSS CORS 的 ExposeHeaders 需包含 `ETag`（分片合并要读分片 ETag）。
4. **注册/缓存优化**：后端 OSS client 按环境变量缓存，配置变化自动重建。
5. **测试**：后端 25、前端 100 个测试全部通过；lint、构建、覆盖率门槛全绿。
