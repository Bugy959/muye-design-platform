# 木叶义齿设计平台 · 管理端使用说明

> 适用对象：平台管理方（最高权限）
> 版本：V2.9（2026-08-21）

---

## 一、登录

管理账号：`admin` / `muye2026`（演示账号，正式使用时请第一时间改密）。
登录后默认进入「账号管理」。

管理端共 8 个标签：**账号管理 → 账单中心 → 积分管理 → 设计师分组 → 订单概览 → 分组匹配 → 返工审核 → 订单总览**。

---

## 二、账号管理（默认页）

所有账号都由你在这里开通，医院和设计师**不能自己注册**。

- **创建账号**：点右上角「+ 创建账号」→ 选端口（医院/加工厂 或 设计师）→ 选"新建档案"或"关联已有档案" → 填账号密码和档案信息（医院：单位名+电话；设计师：姓名+电话+身份证号，执业证号选填）
- 列表分三类展示：**管理端账号置顶单独一块**，下面分「医院/加工厂」和「设计师」两类
- 支持**搜索**（账号名/单位名/姓名），每类**每 15 条自动分页**
- 每行可**重置密码**（对方需重新登录）和**删除账号**（admin 不可删）

---

## 三、积分管理

- 医院微信转账给你后，在这里找到该客户 →「充值」填金额和备注
- 「扣减」用于纠错；**扣减不能超过当前余额**（超了会拦截提示），积分不可能出现负数
- 每个客户名旁边有「参数」小按钮 → 配置该客户的**设计参数默认值**（内冠间隙 / 咬合切 / 邻接切），设计师接单卡片上会显示这条绿色参数条

---

## 四、设计师分组 与 分组匹配

**设计师分组**：建组、改组名、设备注、设组长、把设计师移进/移出组；未分组的设计师单独列在最下面（他们登录后看不到任何订单）。

**分组匹配**：客户分组 × 设计师分组的矩阵，勾选即建立匹配。
- 客户下的单只会进入**匹配到的设计师组**的接单大厅
- 客户组没匹配任何设计师组时，订单进入「未分配」状态，订单总览会黄色警示，配好匹配后到该订单点「重新派发」

---

## 五、订单概览（可点击的统计卡）

6 张卡片：总订单 / 本月新增 / 已完成 / 返工 / 退回 / **超时未接（24h）**。

**点卡片直接跳到订单总览并自动套好筛选**。"超时未接"卡变红说明有超过 24 小时没人接的单，需要督办或调整匹配。

---

## 六、返工审核

医院的返工申请提交后，**订单会立即回到原设计师手上重做**——你这里的审核只做两件事：**登记 + 决定是否退积分**。

- **通过（退积分）**：该单积分全额退回医院并记流水，订单继续由原设计师重做
- **不通过**：不退积分，订单仍由原设计师修改
- 两种结果都会自动发消息通知医院

---

## 七、订单总览（全部订单的管理台）

- 可见**所有订单**的全部信息（派单来源、患者、设计师、照片、扫描/设计文件）
- 顶部按状态筛选（全部/待接单/设计中/已完成/已退回/返工/未分配/已撤回）
- 搜索：单号 / 患者姓名 + 日期范围；点客户名可只看该客户
- 「超时未接（24h）」和「返工订单」两个快捷筛选按钮
- 排序切换：最新 / 最早 / 积分高 / 按状态
- 展开订单：底部有**操作时间线**（提交/接单/完成/退回/返工/审核/撤回全记录）
- **导出订单 CSV**：按当前筛选结果导出（含患者列）

## 八、账单中心

- 按月汇总每个医院/加工厂的订单账单（含基台列），设计师账单对外只显示"X 师傅"
- 可导出月度账单 CSV

---

## 九、日常工作流建议

1. 医院微信转账 → 「积分管理」充值
2. 新客户合作 → 「账号管理」建号建档 → 「分组匹配」勾选匹配
3. 每天看一眼「订单概览」：超时未接变红就督办
4. 「返工审核」有待审红点就处理：理由正当退积分，不正当驳回
5. 月底「账单中心」导出 CSV 对账

---

## 十、注意事项

- 积分 = 钱，所有积分变动都有流水，但**请定期导出账单备份**
- 删除账号不会删除历史订单，但订单上该账号的名字会显示"档案已删除"
- V2.9 起数据默认存服务器数据库（muye.db），换设备不丢数据；顶栏「演示数据」按钮可切换到纯本地演示模式
