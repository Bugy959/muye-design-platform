# 测试补充说明（小白版）+ 服务器部署清单

> 本文档写给对代码不熟的同学。先解释"这次改了什么、为什么"，再给"过段时间上服务器怎么做"的完整清单。
> **更新时间：2026-08-25（V2.10 实测：后端 30 / 前端 103 全绿，lint / build / 覆盖率门槛均通过）**

---

## 第一部分：这次改了什么？

### 1.1 先搞懂两件小事

这个项目由两大部分组成：

- **website（前端）**：用户打开网页看到的内容（医院下单、设计师接单、管理端审核）。
- **server（后端）**：一台"账房"，帮前端保管数据（账号、订单、积分、流水、消息）。数据库是 SQLite（一个文件 `server/data/muye.db`）。

**"测试"是什么？**
你可以把测试想成一位"自动检查员"。每改一次代码，跑一遍 `npm test`，如果破坏了某个功能，检查员会立刻喊"第 X 个检查不通过"。没有测试的话，改坏了自己很难发现。

### 1.2 这次为什么要补测试

我检查了全仓库的"行覆盖率"（= 有多少行代码被测试真正跑过），发现几块空白：

| 位置 | 改动前覆盖率 | 缺什么 |
|---|---|---|
| 后端 routes.js（业务接口） | 92% | 管理端几个功能没测：建/改设计师组、调组、删客户组、删匹配规则、消息"全部已读"、服务器报错兜底 |
| 前端 api.ts（调接口的 30 个函数） | **80%** | 一大半函数从没被测试碰过 |
| 前端 store.ts（核心业务逻辑） | 96% | 建设计师账号、几个"后端模式"异步分支、异常数据容错等边角 |

### 1.3 我具体加了哪些测试（文件级清单）

**后端 `server/tests/api.test.js`（追加了 7 个测试）**

| 新增测试 | 验证什么 |
|---|---|
| 设计师分组 创建/改名/设组长/改备注 | 分组管理接口正常，空组名报 400、不存在的组分 404 |
| 设计师调组并自动解除组长身份 | 组长调去别的组后，旧组组长自动清空（防止一人在两个组当组长） |
| 客户分组 改名与删除 | 有客户的组不能删；空组删除时把它绑定的匹配规则一起清掉 |
| 移除分组匹配规则 | 客户组与设计师组的绑定关系能删掉 |
| 同时新建设计师账号 | 建账号的同时创建设计师档案，新账号能正常登录 |
| 消息"整组标记已读" | 一个医院点"全部已读"，其它医院的消息不受影响 |
| 服务器内部错误兜底 | 传非法数据时接口返回 500 JSON，而不是崩溃或泄露堆栈 |

**前端**

- 新建 `website/tests/api-endpoints.test.js`：把 `api.ts` 里 30 个接口函数全部测了一遍（地址、方法、登录令牌、请求体、返回值）。之前 80% 覆盖率就是它们拖低的。
- `website/tests/core.test.js`：补 `scopeLabel`（订单范围文案）的默认分支。
- `website/tests/core-accounts.test.js`：补"建账号同时新建设计师档案"。
- `website/tests/core-sync.test.js`：补后端模式几个异步函数（建客户组/设计师组/账号）的三种情况：正常走后端、演示模式走本地、没登录时报错。

### 1.4 修改后的效果

| 文件 | 改动前 | 改动后 |
|---|---|---|
| 后端 routes.js | 92.11% | **100%** |
| 前端 api.ts | 80.65% | **96.77%** |
| 前端 store.ts | 96.37% | **99.50%** |
| 后端全部文件 | 93.81% | **99.34%** |
| 前端全部文件 | 95.21% | **99.27%** |

测试数量（2026-08-25 实测）：**后端 30 个全通过；前端 103 个全通过**（含 OSS 分片直传、上线安全加固、请求超时与上传进度用例）。

剩下没覆盖的几行都是"正常情况下跑不到"的防御代码（比如浏览器崩溃时的兜底、React 内部钩子），不影响使用。

### 1.5 注意事项

- **我没有改任何业务代码**，只新增/追加了测试文件，不会影响平台功能。
- 仓库里本来就有一堆别人改了一半没提交的文件（比如删掉的 `components/ui/*`），**那些不是我改的**，测试也没动它们。
- 跑测试时偶尔会打印红色错误日志，那是**故意测试报错场景**（比如"扣分不能为负"），属于正常输出，只要最后显示 `pass 103` / `pass 30` 就是通过（2026-08-25 实测）。

### 1.6 怎么自己跑这些测试（本地）

```bash
# 后端
cd server
npm test

# 前端
cd website
npm test

# 前端覆盖率门槛（要求全部 ≥90%，不达标会失败）
cd website
npm run test:coverage
```

push 到 GitHub 的 main 分支后，`.github/workflows/ci.yml` 会自动跑一遍（lint → 构建 → 测试 → 覆盖率），全绿才放心。

---

## 第二部分：服务器部署清单

> 目标：把平台放到一台公网服务器上，别人能用手机/电脑访问。
> 推荐方案：**nginx 反向代理**（80/443 端口对外，内部转发给后端 3001），前端是构建后的静态文件。
> 更详细的规划（OSS、备案、备份）见仓库里《服务器部署整改方案.md》。

### 第 0 步：动手前（准备阶段）

- [ ] 把当前代码提交并推送到 GitHub main（先保证 CI 全绿再部署）
- [ ] 准备一台服务器：推荐 Ubuntu 22.04/24.04，2 核 2G 起步
- [ ] 注册域名并完成 ICP 备案（国内服务器强制要求，否则 80/443 端口会被拦）
- [ ] 记录服务器公网 IP，备用

### 第 1 步：服务器安装环境（一次性）

```bash
# 安装 Node.js 22 LTS（本项目要求 ≥ 22.13，内置 SQLite 支持）
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
sudo apt-get install -y nodejs git nginx

# 安装 pm2（进程守护：后端崩了自动重启）
sudo npm install -g pm2

node -v   # 确认 ≥ v22.13
```

### 第 2 步：把代码拉到服务器

```bash
cd /var/www
sudo git clone https://github.com/Bugy959/muye-design-platform.git muye
cd muye

# 安装依赖（前后端各一份）
cd server && npm install && cd ..
cd website && npm install && cd ..
```

> 提示：以后更新代码就 `git pull` + 重跑第 3、4 步。

### 第 3 步：构建前端（生成静态网页到 dist 目录）

关键：**让前端用同源路径调接口**，这样不需要处理跨域。

```bash
cd /var/www/muye/website
# 构建时把接口地址写死为 /api（前端代码会拼成 /api/xxx）
VITE_API_BASE=/api npm run build
# 产物在 website/dist，稍后交给 nginx 托管
```

### 第 4 步：启动并守护后端

```bash
cd /var/www/muye/server

# 方式 A：直接起（前台，窗口关了就停，适合先试）
npm start

# 方式 B：用 pm2 守护（推荐，正式用这个）
pm2 start npm --name muye-server -- start

# 保存 pm2 配置，并设置开机自启
pm2 save
pm2 startup   # 按提示执行它输出的那一行命令

# 验证后端活着
curl http://localhost:3001/api/health
# 应看到 {"ok":true,...}
```

> 首次启动会自动创建数据库 `server/data/muye.db` 并写入演示账号（admin/muye2026 等），**上线前务必改掉**（见第 7 步）。

### 第 5 步：配置 nginx（把前端和后端串起来）

新建 `/etc/nginx/sites-available/muye`：

```nginx
server {
    listen 80;
    server_name 你的域名.com;

    # 前端静态文件（第 3 步构建产物）
    root /var/www/muye/website/dist;
    index index.html;

    # 单页应用：找不到文件时回退到 index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 后端接口：/api/xxx 转发到本地 3001
    location /api/ {
        proxy_pass http://127.0.0.1:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

启用并测试：

```bash
sudo ln -s /etc/nginx/sites-available/muye /etc/nginx/sites-enabled/
sudo nginx -t          # 配置检查，OK 再继续
sudo systemctl reload nginx
```

### 第 6 步：配置 HTTPS（必做，给网站加把锁）

```bash
sudo apt-get install -y certbot python3-certbot-nginx
sudo certbot --nginx -d 你的域名.com
# 按提示输入邮箱，选择"重定向 HTTP → HTTPS"
# certbot 会自动改 nginx 配置并开启自动续期
```

完成后访问 `https://你的域名.com` 应能看到登录页。

### 第 7 步：安全清单（上线前必做）

- [ ] **改掉所有演示账号密码**：`admin`（muye2026）、`mingzhou/hengmei/yahe`（123456）、`li/wang/zhao/sun/zhou`（123456）。最省事：登录后用对应角色改密码，或删掉不用的演示账号。
- [ ] **CORS 收紧**：`server/src/index.js` 里有一段允许任意来源跨域的代码（注释写着"开发期允许"），生产环境改成只允许你的域名：
      `res.setHeader('Access-Control-Allow-Origin', 'https://你的域名.com')`
- [ ] **防火墙只开必要端口**：如果走 nginx，只开 80/443，不要对外暴露 3001。
      ```bash
      sudo ufw allow 80,443/tcp
      sudo ufw enable
      ```
- [ ] 大文件（口扫/照片）已走 OSS 直传（代码就绪 2026-08-23，待配置环境变量）；后端 JSON 限制已从 100mb 调回 10mb，nginx 请求体上限保持默认即可——完整步骤见《服务器部署详细指南.md》第 12 章。

### 第 8 步：每日自动备份数据库（务必做）

**整个平台的数据只有一个文件：`server/data/muye.db`**。每天备份一份：

```bash
# 创建备份脚本
mkdir -p /var/backups/muye
sudo tee /etc/cron.daily/muye-backup >/dev/null <<'EOF'
#!/bin/sh
# WAL 模式下直接 cp 可能拷到未合并的事务，改用 SQLite 在线备份 VACUUM INTO（无需停服）
sqlite3 /var/www/muye/server/data/muye.db "VACUUM INTO '/var/backups/muye/muye-$(date +%F).db'"
find /var/backups/muye -name 'muye-*.db' -mtime +30 -delete
EOF
sudo chmod +x /etc/cron.daily/muye-backup
```

> 首次使用需先安装 `sqlite3` CLI：`sudo apt-get install -y sqlite3`。
> 恢复方法：把备份文件拷回 `server/data/muye.db`，重启后端即可。建议每月把备份文件下载到本地电脑一份，并做一次恢复演练（用备份文件能正常启动）。

### 第 9 步：上线验证（按顺序做一遍）

- [ ] `curl https://你的域名.com/api/health` 返回 `{"ok":true}`
- [ ] 网页能打开、能登录（admin 先用临时密码）
- [ ] 走一遍核心流程：医院下单 → 设计师接单 → 提交设计 → 医院看到"已完成"消息
- [ ] 管理端：积分调整、分组管理、账号创建都点一遍
- [ ] 手机开飞行模式换 4G 再访问一次（确认不是只在本机好使）

### 第 10 步：以后更新版本怎么做

```bash
cd /var/www/muye
git pull                       # 拉新代码
cd website && VITE_API_BASE=/api npm run build   # 重新构建前端
cd ../server && npm install    # 如有新依赖
pm2 restart muye-server        # 重启后端
sudo systemctl reload nginx    # 静态文件已更新，通常无需这步，保险起见执行
```

---

## 附：常见问题（FAQ）

**Q1：前端页面打开了，但一直提示"连不上服务器/请登录"？**
大概率是前端没用 `/api` 路径。检查第 3 步是否用了 `VITE_API_BASE=/api` 构建；以及 nginx 的 `/api/` 转发是否生效（`curl https://域名/api/health`）。

**Q2：3001 端口被占用怎么办？**
`pm2 status` 看是不是重复起了多个实例；或改端口：`PORT=3002 pm2 start npm --name muye-server -- start`（同时改 nginx 转发目标）。

**Q3：数据库能直接拷走 / 换服务器吗？**
能。SQLite 是单文件，把 `server/data/muye.db` 拷走，放在新服务器的同样位置，启动即用（注意先停后端再拷，避免拷到一半）。

**Q4：测试红了怎么办？**
先看是不是你改了业务代码导致的。没改过代码还红，把报错信息截图发出来求助。