# 木叶义齿设计平台 · 上线速查清单（购买后 30 分钟版）

> 目标：从买服务器到 HTTPS 可访问、大文件走 COS，30 分钟内跑通主流程。
> 详细步骤见《上线部署详细指南.md》；深挖（COS CORS、备份恢复演练等）见《服务器部署详细指南.md》。

## 0. 购买与前置（下单那一刻）

- [ ] **服务器**：轻量应用服务器 **4核4G / 40GB SSD / 3Mbps / 300GB 流量**
- [ ] **镜像**：系统镜像 → **Ubuntu 22.04 LTS（64 位）**（⚠️ 别选“Node.js 22.12.0”应用镜像，缺 node:sqlite）
- [ ] **域名**（如 muye-design.com）
- [ ] **ICP 备案**：在**腾讯云备案系统**提交（2~3 周，最慢的一环，**现在就去**）
- [ ] **SSL 证书**：不买，用免费证书（certbot / 云厂商免费 DV）
- [ ] **COS**：开通私有桶 `muye-1250000000`（名字-APPID 格式），Region `ap-shanghai`
- [ ] **API 密钥**：新建 SecretId / SecretKey（建议子账号最小权限）

## 1. 服务器基础（SSH 登录后，5 分钟）

- [ ] `sudo apt update && sudo apt install -y nginx sqlite3 git`
- [ ] 装 Node 24（nvm）：`curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.1/install.sh | bash` → 重开终端 → `nvm install 24`
- [ ] `npm install -g pm2`
- [ ] 防火墙/安全组只放行 80、443（SSH 22 看你需要）

## 2. 部署代码（10 分钟）

- [ ] `git clone https://github.com/Bugy959/muye-design-platform.git /var/www/muye && cd /var/www/muye`
- [ ] 前端：`cd website && npm ci && npm run build`（`.env.production` 已内置 `VITE_API_BASE=/api`）
- [ ] 后端：`cd server && npm ci`
- [ ] `cp ecosystem.config.example.cjs ecosystem.config.cjs` 并填入密钥（COS_*、CORS_ORIGIN，见详细指南）→ `cd server && pm2 start ecosystem.config.cjs && pm2 save && pm2 startup`
- [ ] 冒烟：`curl http://127.0.0.1:3001/api/health`

## 3. nginx + HTTPS（10 分钟）

- [ ] 写 `/etc/nginx/sites-available/muye`（前端静态 + `/api` 反代 + SPA fallback，请求体默认 1m 即可）
- [ ] `sudo ln -s /etc/nginx/sites-available/muye /etc/nginx/sites-enabled/ && sudo nginx -t && sudo systemctl reload nginx`
- [ ] `sudo apt install -y certbot python3-certbot-nginx`
- [ ] `sudo certbot --nginx -d 你的域名.com`（自动配 HTTPS + 自动续期）

## 4. 收尾与验证（5 分钟）

- [ ] 改掉/删除种子账号：admin（muye2026）、mingzhou/hengmei/yahe、li…（123456）
- [ ] `CORS_ORIGIN=https://你的域名.com` 已写入 ecosystem 并重启 pm2
- [ ] 每日备份 cron（`VACUUM INTO`，见详细指南第 7 节）已生效
- [ ] `curl https://你的域名.com/api/health` → `{"ok":true,"db":true,"cos":true,...}`
- [ ] 走完整流程：医院下单（实测上传口扫）→ 设计师接单 → 交稿 → 医院下载设计文件
- [ ] 手机切 4G 访问一次确认不只在服务器本机好使

## 预期时间

| 环节 | 用时 |
|---|---|
| 服务器 + COS + 密钥购买配置 | 10~20 分钟（含等待） |
| 1 服务器基础 | ~5 分钟 |
| 2 部署代码 | ~10 分钟 |
| 3 nginx + HTTPS | ~10 分钟 |
| 4 收尾验证 | ~5 分钟 |
| **合计** | **约 30~50 分钟**（不含备案 2~3 周并行进行） |