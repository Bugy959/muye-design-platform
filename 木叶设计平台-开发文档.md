# 木叶义齿设计平台 · 开发文档

连接医院/加工厂与远程设计师的数字化建模协作平台

**版本：V2.10 | 更新日期：2026年8月25日**

V2.10 变更（2026-08-25）：上线安全加固（P0）——CORS 支持 `CORS_ORIGIN` 环境变量白名单（未配置默认开发期放行）；登录新增限流（默认 5 次/分钟/IP，`LOGIN_MAX_PER_MINUTE` 可调）；接入 helmet 全局安全响应头；`express.json` 限制从 100mb 调回 10mb（`JSON_BODY_LIMIT` 可覆盖，小文件 dataUrl ≤2MB、大文件已走 OSS）；`TRUST_PROXY` 支持 nginx 反代下按客户端真实 IP 限流。前端第一轮：新建 `website/.env.production`（`VITE_API_BASE=/api`，走 nginx 同源反代）；下单表单扫描文件/照片上传显示**进度条**（`readOrderFile(file, onProgress)` 透传百分比，>50MB 分片逐片推进）；`apiFetch` 内置 **20s 超时**（`fetchWithTimeout`，超时抛友好错误，OSS 直传不受影响）。全仓文档统一口径：后端 30 / 前端 103 测试全绿，部署规格定稿 4核4G/40GB SSD/3M/300GB 流量，数据库备份改用 `VACUUM INTO`。

V2.9 变更（2026-08-21）：质量清单 P0 完成——前端数据层对接后端 API。新增 `lib/api.ts` 统一 API client（31 个接口封装、Bearer token、统一错误处理）；store.ts 新增后端同步层（本地乐观更新 + 同步 API + `/api/bootstrap` 覆盖刷新，失败自动回滚为服务端数据）；登录/退出与三端全部业务操作对接后端；App 增加后端初始化（加载页 / 连接失败页可重试、可切演示数据）与 15 秒轮询；抢单失败提示服务端消息（如"手慢了"）；后端补 `POST /api/notices/:id/read`（含越权测试），`moveDesigner` 对齐演示版"调离原组自动解除组长"；顶栏「演示数据」按钮（演示模式下显示，点击切回后端）/ localStorage `muye-data-mode=demo` 切入演示模式；全端口牙位展示统一为 FDI 编号（如 21、23），不再显示"左上/左下"等方位字眼；start.bat 改为同时拉起后端（:3001）与前端（:3000），修复打开页面无法连接后端的问题。另补后端同步层测试 18 个（tests/core-sync.test.js，mock window + fetch，覆盖乐观更新/失败回滚/刷新合并），覆盖率门槛通过（store.ts 96.37% / 全量 95.25%）；后端补「医院读他人订单」越权用例（bootstrap 数据隔离 + 跨医院撤回/返工被拒）。lint / build / 前端 71 测试（53 核心 + 18 后端同步层）/ 后端 14 测试 / 冒烟联调全绿。

V2.8 变更（2026-08-19）：质量清单 P1 + P3 完成——前端 lint 45 个问题清零（0 error / 0 warning），删除 50 个未使用的 shadcn 模板组件（仅保留 alert-dialog / button / sonner），`fmtSize`/`sortOrders` 迁至 `lib/order-utils.ts`、`buttonVariants` 迁至 `ui/button-variants.ts`，新增 `lib/use-now.ts`；CI 增加 `npm run lint` 与覆盖率门槛（`tests/check-coverage.mjs`，行覆盖 ≥ 90%；2026-08-21 实测 store.ts 96.37% / 全量 95.25%（V2.9 补后端同步层测试后）），lint / test / build / coverage 任一失败即拦截。

V2.7 变更（2026-08-19）：新增《前端改造与质量清单.md》，明确前端 P0-P3 改造项：数据层对接后端 API、修复 lint 红灯、补组件/交互测试、CI 质量收口。

## 一、项目概述

木叶义齿设计平台连接三类用户：医院/加工厂（下单客户）、远程设计师（接单设计）、平台管理方。平台仅提供数字化建模技术服务与交接环境，不对最终义齿临床效果、医疗器械质量承担责任（登录页与各端底部均有免责声明小字）。

当前状态：**前端已对接后端 API（P0 完成，2026-08-21）；上线安全加固完成（2026-08-25，V2.10）；待配置 OSS 环境变量与部署上线**。

- 前端：三端 SPA，已对接后端 API（2026-08-21，见 V2.9）；localStorage（key：`muye-design-platform-v5`）仅作演示模式与本地缓存
- 后端：`server/` 目录（2026-08-05 完成，2026-08-21 补单条消息已读等收口），Node 24 + Express 4 + 内置 SQLite，全部业务 API 已实现并与前端规则同步，烟测通过；下一步是部署上线（详见根目录《服务器部署整改方案.md》）

V2.3 变更（2026-08-08）：返工流程按老板最终规则调整（申请即回原设计师重做，审核只管退积分）；管理端积分扣减禁止扣成负数；修复审计发现的 18 项文档/代码不一致。

V2.4 变更（2026-08-13）：设计师端接单大厅由半宽大卡片改为通栏长条（120px 高，一屏 6~7 单，尺寸经老板预览确认）；新增迷你牙位图（十字分区线+四象限角标+选中编号直出，防数错）；长条点击展开详情；接单大厅与我的订单均新增「全部下载」按钮，一键下载该单全部扫描文件与照片。

V2.5 变更（2026-08-19）：牙位图统一为每侧 8 颗（含智齿 18/28/38/48），医院端选择图与设计师端迷你图保持一致，并修复迷你图多余空白格。

V2.6 变更（2026-08-19）：工程与体验优化——消息支持单条已读；Vite 代码分包（React/shadcn/业务三块）；新增 Vitest 核心纯函数测试；新增 Git 版本管理 + .gitignore；新增 GitHub Actions CI（tsc + build）。复用上次订单、订单详情抽屉、Ctrl+K 全局搜索、前后端类型共享标记为待后续迭代。

## 二、技术栈

| 层面 | 技术 | 版本 |
|---|---|---|
| 前端框架 | React + TypeScript | 19.2 / ~5.9 |
| 构建 | Vite | 7.3 |
| 样式 | Tailwind CSS + shadcn/ui | 3.4 |
| 前端数据 | 后端 API + useSyncExternalStore（localStorage 仅演示模式/缓存） | lib/api.ts 自实现 |
| 后端 | Node.js + Express | 24 / 4.x |
| 数据库 | SQLite（node:sqlite 内置） | 单文件 muye.db，13 张表 |

## 三、牙位编码

FDI 双位数体系：上右 11-18、上左 21-28、下左 31-38、下右 41-48（每侧 8 颗，含智齿）。牙位图排布：左半 上 1x / 下 4x，右半 上 2x / 下 3x（老板亲定）。旧编码（U/D+L/R+n）在 ensureDBShape 启动时自动迁移。

医院端牙位选择支持「自定义颗数」：可一次性选 20、30 颗并附带多张照片（照片最少 1 张，数量不限）。

## 四、设计类型与积分

| 类型 | 积分 | 备注 |
|---|---|---|
| 即刻设计 (jike) | 8 分/颗 | 即"加急件"，页面明显标注（红色徽章） |
| 全瓷冠/基台上部冠 (quanci) | 5 分/颗 | 支持自定义颗数 |
| 贴面/嵌体 (tiemian) | 10 分/颗 | 加急 +5 分/颗 |
| 马龙桥 (malong) | 80 分/件 | 按件收费，上颌/下颌/全口 |
| 基台 (jita) | 5 分/颗 | |

积分规则：

- 客户微信转账给管理方，管理端手动充值/扣减积分；积分解释权归管理方所有，无时间限制
- **积分不允许扣成负数**：管理端扣减超过余额时拦截并提示（前端演示版与后端 API 行为一致）
- 下单预扣积分；积分不足时无法下单，提示"积分不足，请联系管理方充值"
- 撤回订单 / 返工审核通过：预扣积分全额退回并记流水
- 退回后重新提交：多退少补
- **积分可见性**：医院端可见积分余额、积分明细与订单积分价格（订单行直接显示）；设计师端全站不可见积分；管理端全量可见

## 五、导航架构

- 医院端：设计师设计 → AI 设计 → 用户服务 → 积分明细 → 消息 → 我的订单（最右，通栏长条 + 迷你牙位图 + 点击展开详情）
- 设计师端：接单大厅 / 我的订单均为通栏长条（120px + 迷你牙位图 + 点击展开详情 + 一键全部下载），我的订单页首含 4 格统计卡片
- 管理端：账号管理（默认）→ 账单中心 → 积分管理 → 设计师分组 → 订单概览 → 分组匹配 → 返工审核 → 订单总览

「AI 设计」为占位页，显示"功能正在开发中，敬请期待"。

## 六、订单状态机

| 状态 | 含义 | 进入方式 |
|---|---|---|
| pending 待接单 | 已进接单大厅 | 下单且客户组有匹配设计师组 |
| unassigned 未分配 | 无匹配设计师组 | 下单但无匹配；管理端可重新派发 |
| designing 设计中 | 设计师已抢单 | 设计师接单 |
| returned 已退回 | 设计师退回（信息不全/数据问题） | 设计师点退回（记录 returnedAt） |
| completed 已完成 | 设计师提交设计稿 | 交稿即完成（订单完成标准） |
| rework 返工重做中 | 订单在原设计师手上重做 | 客户申请返工（**保留原设计师，不进大厅、不可被抢**） |
| cancelled 已撤回 | 客户主动撤回 | 记录 cancelledAt，积分全退 |

每张订单带完整时间线（组件 OrderTimeline）：提交 → 接单 → 完成；退回/返工为红色终态节点，撤回为灰色终态节点；管理端订单详情底部有「操作时间线」，列出提交/接单/完成/退回/返工申请/审核结果/撤回等全部事件。

撤回规则：只要还没有设计师接单（pending / unassigned），客户即可撤回，预扣积分全额退回，订单立即从接单大厅消失。

## 七、返工流程（V2.3 老板最终规则）

1. 客户从已完成订单发起返工（留言 + 照片）→ **订单立即回到原设计师手上重做**（状态 rework，不进接单大厅、其他设计师看不到也抢不到）；设计师在「我的订单」看到返工要求与附图，重新提交设计文件即再次完成（返工次数 +1）
2. 管理端审核仅做两件事：**登记** + **决定是否退还该单积分**
   - 通过：退还全额积分并记流水、通知客户；订单仍留原设计师重做
   - 不通过：不退积分、通知客户；订单仍留原设计师修改
3. 审核前客户可在「用户服务」修改返工申请（留言 + 重传照片）或撤销申请（订单恢复已完成；若此前有过返工记录则保留返工标记）

## 八、分组匹配

管理端将客户和设计师归类分组，通过 GroupAssignment 表（M:N）控制订单可见性。客户组无匹配时订单进入 unassigned 状态，管理端可重新派发。分组名由管理方自定义（不做地区标签）。

## 九、设计参数

管理端为每个客户配置内冠间隙 / 咬合切 / 邻接切默认值（0.02 / 0.1 / -0.02），设计师端接单卡片以品牌绿横条展示。

## 十、搜索、排序与导出

- 医院端「我的订单」：单号/患者姓名搜索 + 日期范围 + 排序切换（最新/最早/积分高/按状态）
- 管理端「订单总览」：单号/患者姓名搜索 + 日期范围 + 状态筛选（含已撤回/未分配）+ 超时未接一键筛选（超过 24 小时未接的待接订单标红）+ 返工订单一键筛选 + 点客户名筛选 + 排序切换 + 导出 CSV（按当前筛选结果，含患者列）
- 管理端「账单中心」：按月账单导出 CSV（含基台列）
- 管理端「订单概览」6 张统计卡片（总订单/本月新增/已完成/返工/退回/超时未接）**可点击**，直达订单总览并自动套好对应筛选；其中「返工」卡统计所有发生过返工的订单，跳转后也是同一口径（isRework 筛选）

## 十一、消息通知

医院端「消息」页接收系统通知：

- 订单完成提醒
- 订单被退回提醒
- 返工审核通过 / 不通过提醒

进入消息页即自动全部标为已读（导航上显示未读条数）。

## 十二、隐私与权限规则

- 设计师端：可见患者姓名/编号，**不可见**医院名称、电话、积分；账单与导出仅显示"X 师傅"
- 医院端：可见自己的积分余额、积分明细、订单积分价格（订单行直接显示积分）
- 管理端：可查看所有订单（含上传的照片与文件）、所有账号、全部积分流水
- 账号管理：医院/加工厂与设计师分类展示，admin 管理账号单独置顶；支持搜索，每类每 15 条分页
- 上传文件不限格式；扫描文件与照片分开下载；照片点击放大（Lightbox）；文件显示大小（扫描文件蓝色系、照片绿色系）
- 演示版说明：数据隔离靠界面层不渲染（localStorage 架构限制）；后端 API 已实现真正的角色级数据隔离（医院只见自己 / 设计师不见医院与积分 / 管理全量）

## 十三、演示账号

| 端口 | 账号 | 密码 | 说明 |
|---|---|---|---|
| 管理端 | admin | muye2026 | 全部功能 |
| 医院/加工厂 | mingzhou / hengmei / yahe | 123456 | 不同客户组 |
| 设计师 | li / wang / zhao / sun / zhou | 123456 | A-D 各组 |

## 十四、UI 设计系统（2026-08-06 融合方案定稿，08-08 圆角落实）

| 项目 | 内容 |
|------|------|
| 设计基调 | 暖纸面 + 品牌绿（主色与背景不变） |
| 字体 | Figtree（统计数字标签）+ PingFang SC / Microsoft YaHei（中文正文） |
| 配色 | 暖白纸面 #fdfaf6 + 品牌绿 #1e5c46 + 琥珀强调 #D97706（待处理/超时）；**加急件徽章为红色 #b3402f**（更醒目，已上线沿用） |
| 圆角层次 | 按钮/输入框 4px · 卡片 6px（rounded-md）· 弹窗 8px（rounded-lg）· 徽章/标签全圆角（08-08 已全站落实） |
| 阴影层次 | 卡片 shadow-sm · hover shadow-md · 弹窗 shadow-lg（中性灰，不用彩色投影） |
| 导航 | 三端统一底部 2px 品牌绿指示线（激活项），非激活项无背景 |
| 表格 | 表头 2px 底线、偶数行暖石淡底、hover 行底色 |
| 分界线 | 全站 stone-300（2026-08-06 老板反馈太淡后加深一档） |
| 时间线 | 退回/返工为红色终态节点；**撤回为灰色终态节点**（非错误含义） |
| 焦点 | 输入框 3px 品牌绿焦点环；全局 focus-visible 轮廓 |
| 触控 | 主按钮 min-height 44px；全局 cursor-pointer；整体字号偏大（服务牙医群体） |
| 无障碍 | prefers-reduced-motion 动画偏好 |
| 明确不做 | 卡片彩色左边线、绿色投影、品牌色变更、背景纹理、额外字体族 |

## 十五、V2.1 优化清单（已收口）

20 项 UI/UX 优化最终状态：**15 完成 / 4 不实施（第 14/16/19/20 项）/ 1 待定（第 1 项"复用上次订单"，待老板拍板）**。详见 V2.1-UX待优化清单.md。

## 十六、上线待办

详见根目录《服务器部署整改方案.md》。剩余路线：

1. ~~**前端切换到后端 API**~~ ✅ 已完成（2026-08-21，见 V2.9）
2. ~~STL 大文件对象存储（OSS）~~ ✅ 代码已实现（2026-08-23），待配置 OSS 密钥后启用
3. 购买服务器 + 域名 + ICP 备案
4. 数据库定期备份（复制 muye.db）


## 十七、大文件上传（OSS 直传，2026-08-23）

口扫文件/照片/设计稿体积大（可达 500MB-1GB），不放进数据库/JSON，走**对象存储直传**（阿里云 OSS / 腾讯云 COS），本服务器只做"签发凭证"和"文件登记"。

### 十七.1 数据流

1. 前端选文件 → `readOrderFile`：≤1.5MB 小文件仍内嵌 dataUrl（本地预览/演示模式）；大文件走第 2 步
2. 后端模式 + 大文件：`uploadOrderFile(file)` 自动选择——≤50MB 单次直传（`POST /api/files/upload-token` → PUT OSS）；>50MB **分片直传**（`/files/upload-init` → 逐片 `/files/upload-part-url` PUT → `/files/upload-complete`），每片 5MB、并发 4、**断点续传**（完成的分片写入 localStorage 检查点，失败/刷新后自动续传；上传预签名有效期 1 小时）
3. 订单 bootstrap 返回时，后端把 `key` 换成短时签名下载地址（`url`），设计师/医院直接点开下载
4. 未配置 OSS 时：上传接口返回 400，前端自动回退旧逻辑（大文件仅记文件名），其它功能不受影响

### 十七.2 涉及文件

| 层 | 文件 | 说明 |
|---|---|---|
| 后端 | `server/src/files.js` | OSS 预签名模块：`getUploadTarget` / `initMultipartUpload` / `getUploadPartUrl` / `completeMultipartUpload` / `getDownloadUrl` / `ossConfigured`（1GB 上限） |
| 后端 | `server/src/routes.js` | `POST /api/files/upload-token` / `upload-init` / `upload-part-url` / `upload-complete`；bootstrap 对含 `key` 的文件签名 |
| 前端 | `website/src/lib/api.ts` | `apiGetUploadToken` / `apiUploadFile` / `apiUploadInit` / `apiUploadPartUrl` / `apiUploadPart` / `apiUploadComplete` |
| 前端 | `website/src/lib/store.ts` | `uploadOrderFile`；`readOrderFile` 大文件自动走 OSS |
| 类型 | `website/src/types/index.ts` | `OrderFile` / `OrderImage` 增加 `key` / `url` |

### 十七.3 环境变量（后端）

| 变量 | 说明 |
|---|---|
| `OSS_REGION` | 地域，如 `oss-cn-hangzhou` |
| `OSS_BUCKET` | 私有存储桶名 |
| `OSS_ACCESS_KEY_ID` / `OSS_ACCESS_KEY_SECRET` | AccessKey |

开通 OSS、CORS、nginx、成本等完整步骤见《服务器部署详细指南.md》第 12 章。
