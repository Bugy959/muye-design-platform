# 木叶义齿设计平台 —— 服务器部署整改方案

> 版本：V1.0（2026-08-05）
> 读者：老板（非技术背景也能看懂）；执行者：后续开发/运维
> 目标：把现在的"本地演示版"整改为"可正式上线的服务器版"，不推倒重来。

---

## 一、现状：为什么不能直接上线

现在 `website/` 是**纯前端演示版**，所有数据存在每个使用者自己电脑的浏览器里（localStorage）。直接给医院和设计师用会有 4 个硬伤：

| # | 风险 | 后果 | 整改方向 |
|---|------|------|----------|
| 1 | 数据各存各的浏览器 | 医院下的单，设计师根本看不见，平台无法运转 | 数据集中到服务器数据库 |
| 2 | 积分存在本地，可被篡改 | 按 F12 就能给自己改成 99999 积分，积分=钱，等于没锁的保险柜 | 积分账目全部服务器管理 + 事务 + 流水 |
| 3 | 密码明文存储 | 任何人可从浏览器翻出所有账号密码 | scrypt 加密存储 |
| 4 | 抢单无并发控制 | 两个设计师同时点接单，都以为自己抢到了 | 数据库原子操作，一单一主 |

另有 2 个现实问题：

- **口扫文件太大**：STL 动辄几十上百 MB，浏览器存不下、传不动 → 用对象存储（OSS/COS）。
- **患者数据敏感**：口扫与口内照片属医疗相关个人信息 → HTTPS 加密传输、权限隔离、保密条款、留存期限（底部免责声明已有，方向正确）。

---

## 二、已完成：后端骨架（2026-08-05）

已新建 `server/` 目录，这是平台的服务器端，**本地即可运行，不等服务器购买**：

```
server/
├── package.json        # 仅 Express 一个外部依赖
├── README.md           # 怎么跑 + API 一览表
├── data/muye.db        # 首次启动自动生成的数据库文件（整个平台的数据都在这一个文件里）
└── src/
    ├── index.js        # 入口（默认端口 3001）
    ├── schema.sql      # 数据库表结构（13 张表）
    ├── db.js           # 建库、种子数据、事务工具
    ├── auth.js         # 密码加密（scrypt）+ 登录令牌
    └── routes.js       # 全部业务 API（规则与演示版 store.ts 一一对应）
```

**已验证（2026-08-05 烟测通过）**：登录 → 医院下单预扣积分 → 设计师抢单（重复抢单被原子锁拦截）→ 提交设计稿 → 医院撤回退积分 → 管理端充值 → 流水逐笔正确，余额分毫不差。

**本地启动方式**：

```bash
cd server
npm install     # 仅首次
npm start       # http://localhost:3001
```

---

## 三、整改路线图（四步）

### 第 1 步：后端骨架 ✅ 已完成

- 账号/订单/积分/流水/消息/分组匹配全部服务端化
- 密码加密、角色权限、抢单锁、积分事务已内置

### 第 2 步：前端切换到后端 ✅ 已完成（2026-08-21）

`website/src/lib/store.ts` 已从"读写浏览器 localStorage"切换为"调用 server 的 API"：本地乐观更新 + 同步 API + `/api/bootstrap` 覆盖刷新，失败自动回滚；三端业务操作全部对接后端。

- **页面组件几乎零改动**（当初把业务逻辑集中在 store.ts 就是为这一步准备的）
- 切换后本地运行方式变为：同时启动 `website/`（前端开发服务器）和 `server/`（后端 :3001）
- 保留演示模式：顶栏"演示数据"按钮 / localStorage `muye-data-mode=demo`
- 前端 API 地址默认跟随浏览器当前主机名（局域网设备自动指向服务器 IP，本机回退 localhost）；生产部署时设 `VITE_API_BASE=/api` 走 nginx 反代

### 第 3 步：文件上云

- 口扫文件/照片/设计稿改为上传对象存储（阿里云 OSS 或腾讯云 COS），数据库只存文件地址
- 上传走"后端签发临时凭证 → 浏览器直传 OSS"，大文件不经过服务器，速度快、服务器压力小
- 下载权限控制：只有本订单的医院、接单设计师、管理端能下载
- 预计工作量：0.5～1 天

### 第 4 步：买服务器、备案、上线

| 事项 | 说明 | 耗时 |
|------|------|------|
| 买云服务器 | 阿里云/腾讯云轻量应用服务器，2核2G 起步即可 | 当天 |
| 买域名 | 如 muye-design.com / 拼音缩写均可 | 当天 |
| **ICP 备案** | **免费，但需 2～3 周，全流程最慢的一环，建议最先启动**；需营业执照（个体户/公司均可） | **2～3 周** |
| HTTPS 证书 | 免费证书（Let's Encrypt / 云厂商免费 SSL） | 半天 |
| 部署 | 服务器上跑 server（用 pm2 守护进程），website 构建产物用 nginx 托管，API 走 /api 反代 | 1 天 |
| 自动备份 | 数据库文件每日打包上传 OSS/另一台机器，保留 30 天 | 半天 |

---

## 四、费用粗估（大致行情，以下单时官网价格为准）

| 项目 | 量级 |
|------|------|
| 轻量云服务器（2核2G） | 约 60～150 元/月（首年常有优惠） |
| 域名 | 约 30～80 元/年 |
| 对象存储 OSS | 量小几乎可忽略，几元/月 |
| HTTPS 证书 | 0 元（免费证书） |
| ICP 备案 | 0 元 |
| **合计** | **约 100～200 元/月即可跑起来** |

> 微信支付接口（积分在线充值）需要企业主体 + 商户号，前期维持"微信转账 + 管理端手动加积分"即可上线，不影响业务。

---

## 五、安全清单（上线前逐项打勾）

- [x] 密码 scrypt 加密，永不存明文
- [x] 登录令牌 7 天有效；改密码/删账号后旧令牌立即失效
- [x] 医院只能读写自己的订单/积分/消息
- [x] 设计师看不到医院信息、看不到积分价格、只能接分组匹配范围内的单
- [x] 积分增减全部数据库事务，余额不足回滚，不会扣成负数
- [x] 抢单原子操作，一单一主
- [x] 每笔积分变动都有流水（谁在何时因何变动、余额多少）
- [ ] HTTPS 全站加密（第 4 步）
- [ ] CORS 跨域白名单收窄到正式域名（第 4 步，现在是开发期放开）
- [ ] 登录失败限次（防暴力破解，第 2 步加）
- [ ] 数据库每日自动备份 + 异地留存（第 4 步）
- [ ] 患者数据保密协议文本（业务侧准备，建议找律师看一眼）

---

## 六、需要老板拍板/准备的事

1. **备案主体**：用个体工商户还是公司营业执照？（决定备案类型，也影响以后能不能开微信商户号）
2. **域名想要什么**：给我 2～3 个候选，我查可注册性
3. **云厂商选哪家**：阿里云或腾讯云都行，没偏好我默认按阿里云出操作步骤
4. **服务器谁管**：上线后需要偶尔续费、看备份是否正常；可以我出一份《运维一页纸》照着做
5. ~~**切换时机**~~ ✅ 已切换（2026-08-21）：演示版功能已定型，前端已对接后端 API

---

## 七、上线检查清单（第 4 步执行时逐项核对）

- [ ] 服务器、域名、备案、HTTPS 四件套就绪
- [ ] server 用 pm2 开机自启 + 崩溃自动重启
- [ ] nginx 托管前端 + /api 反向代理 + HTTPS 跳转
- [ ] CORS 白名单已收窄
- [ ] 数据库每日备份脚本运行正常，恢复演练过一次
- [ ] admin 默认密码（muye2026）已改强密码
- [ ] 演示账号（mingzhou 等）已删除或改密
- [ ] 种子数据库已清空订单（或全新建库）
- [ ] 用真实流程走一遍：医院下单 → 设计师抢单 → 交稿 → 返工审核 → 撤回 → 积分流水核对
- [ ] 底部免责声明核对无误

---

## 八、常见问题（老板可能会问的）

**Q：现在改功能会不会白做？**
不会。页面和交互全部保留，只是把"数据存哪"从浏览器换成服务器。

**Q：以后量大了 SQLite 扛得住吗？**
几十家医院、每天几百单完全没问题。真到上千单/天，表结构已按通用 SQL 设计，可平迁 MySQL，业务代码不用重写。

**Q：服务器坏了数据会丢吗？**
数据库就这一个文件（`muye.db`），每日备份到 OSS/另一台机器，最坏情况丢一天数据。第 4 步会配好自动备份。

**Q：中途想加功能怎么办？**
照旧提需求。前端切后端后，加功能 = 前端加页面 + 后端加接口，和现在节奏一样。
