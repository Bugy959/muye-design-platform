# 木叶义齿设计平台 · 前端改造与质量清单

更新日期：2026年8月21日

## 当前基线

| 项目 | 状态 |
|---|---|
| 前端核心逻辑测试 | ✅ 71 个全部通过（53 核心 + 18 后端同步层 core-sync.test.js） |
| store.ts 行覆盖 | ✅ 96.37%（2026-08-21 新增后端同步层测试后实测） |
| store.ts 函数覆盖 | ✅ 84.92%（行覆盖为门槛项） |
| 前端构建 | ✅ `npm run build` 通过 |
| 前端 lint | ✅ 0 问题（2026-08-19 修复，原 45 个） |
| 前端组件/交互测试 | ❌ 无（P2 待做） |
| 前端数据层 | ✅ 已对接后端 API（localStorage 保留为可切换演示模式，2026-08-21 完成） |
| 后端 API 测试 | ✅ 14 个全部通过（2026-08-21 补「医院读他人订单」越权用例） |
| 前后端冒烟联调 | ✅ 通过（登录 → 下单 → 接单 → 交稿 → 消息已读全链路） |

改造顺序建议：**P0 → P1 → P2 → P3**。

---

## P0：数据层从 localStorage 切换到后端 API（✅ 已完成 2026-08-21）

目标：三端真实操作全部走后端 `/api` 接口，localStorage 只保留为可选演示模式。**结果：lint / build / 前端 108 测试 / 后端 36 测试 / 冒烟联调全部通过（2026-08-25 实测）。**

- [x] 新增统一 API client：`src/lib/api.ts`（fetch 封装、Bearer token、统一错误处理、31 个接口封装）
- [x] 登录 / 退出对接 `/api/auth/login`、`/api/auth/logout`（Login.tsx 后端分支 + 登录中防重复提交）
- [x] 初始化数据对接 `/api/bootstrap`：App.tsx 登录后 `initBackend()` 拉取，加载页 / 连接失败页（可重试、可切演示数据）；之后每 15 秒轮询刷新
- [x] store.ts 后端同步层：变更先本地乐观更新 → 同步调 API → 用 `/bootstrap` 服务端数据覆盖（并发合并刷新；失败自动回滚为服务端数据）
- [x] 医院端：下单（createOrderAsync，展示服务端单号与错误消息）、撤回、退回重提、返工申请、消息已读（单条/全部）对接
- [x] 设计师端：接单（acceptOrderAsync，失败提示"手慢了"等服务端消息）、提交设计、退回对接
- [x] 管理端：积分、账号（创建/改密/删除）、设计师/客户分组、匹配规则、未分配派发、设计参数对接
- [x] 后端补 `POST /api/notices/:id/read` 单条已读接口（含越权测试）；`moveDesigner` 对齐演示版"调离原组自动解除组长"
- [x] 保留并显式切换 localStorage 演示模式：顶栏"演示数据"按钮一键切回后端；localStorage `muye-data-mode=demo` 切演示

验收标准：三端操作后刷新，数据来自后端；演示模式可一键切回，不影响测试。✅ 已达成（Node 测试环境无 window，核心测试走演示逻辑；另新增 `tests/core-sync.test.js` 18 个后端同步层测试，mock window + fetch 覆盖乐观更新/失败回滚/刷新合并，行覆盖门槛通过）

---

- **2026-08-25 前端补充（第一轮）**：新建 website/.env.production（VITE_API_BASE=/api）；下单表单上传进度条（eadOrderFile 透传 onProgress）；piFetch 20s 超时（etchWithTimeout）。

---

## P1：修复前端 lint 红灯（✅ 已完成 2026-08-19）

目标：`npm run lint` 达到 0 error。**结果：0 error / 0 warning，构建与全部测试通过（2026-08-21 起为 71 个）。**

- [x] 修复 `AdminApp.tsx` 中 `Date.now()` 在渲染期间调用（超时未接判断）→ 新增 `src/lib/use-now.ts`，挂载时惰性取一次
- [x] 修复 `bits.tsx` 中 `Stat` 的 setState 写在 effect 内 → 改为 `key={value}` 重挂载重放 CSS 动画；`Orders` 组件的 jump 同步改为渲染期 prev 比较写法
- [x] 将 `AdminApp.tsx` 的 `BillTable` 组件定义移到模块作用域
- [x] 收窄全仓 `any` 类型，使用 `types/index.ts` 现有类型（`ClientApp.tsx` 20 处：`Order` / `OrderImage` / `ReworkRequest` / `Client`）
- [x] 拆分混合导出组件文件，满足 Fast refresh 规则：
  - `bits.tsx` 的 `fmtSize` / `sortOrders` / `OrderSort` 迁出至 `src/lib/order-utils.ts`
  - `buttonVariants` 迁出至 `components/ui/button-variants.ts`
  - 删除 50 个业务未引用的 shadcn 模板组件（仅保留 `alert-dialog` / `button` / `sonner`，未来可按需用 shadcn CLI 重装）
- [x] 修复 `ClientApp` / `DesignerApp` 的 useMemo 依赖（统一依赖整个 `db`，消除 React Compiler 报错与 exhaustive-deps 警告）

验收标准：`npm run lint` 无 error。✅ 已达成（lint exit=0）

---

## P2：补充前端组件/交互测试

目标：关键页面行为有自动化测试，不再只靠人工验证。

- [ ] 引入 Vitest + React Testing Library + jsdom
- [ ] 医院端：下单校验、积分不足提示、撤回、返工申请
- [ ] 设计师端：接单大厅过滤、抢单、提交设计、退回
- [ ] 管理端：账号创建、积分调整、分组匹配、未分配派发
- [ ] 登录与角色权限入口测试

验收标准：核心流程有组件级测试，`npm test` 全绿。

---

## P3：CI 收口（✅ 已完成 2026-08-19）

目标：质量红线在合并前拦住问题。**结果：lint / test / build / coverage 四道门全部进入 CI，本地全绿。**

- [x] CI 增加 `npm run lint`（`.github/workflows/ci.yml` 新增 Lint 步骤）
- [x] CI 统一运行前端测试（沿用 Node test runner：`node --test`，71 个测试 = 53 核心 + 18 后端同步层）
- [x] 增加覆盖率门槛（行覆盖 ≥ 90%）：新增 `tests/check-coverage.mjs` + `npm run test:coverage`；V2.8 时 store.ts 98.77% / 全量 98.94%，2026-08-21 补后端同步层测试（core-sync.test.js，18 个）后实测 store.ts 96.37% / 全量 95.25%，均 ≥ 90%
- [x] lint / test / build / coverage 任一失败即阻止合并（四步任一失败工作流即红）

---

## 备注

- 前端 API 地址默认跟随浏览器当前主机名（`http://<hostname>:3001/api`，局域网设备自动指向服务器 IP），本机回退 `http://localhost:3001/api`；可用 `VITE_API_BASE` 环境变量覆盖（生产 nginx 反代时设为 `/api`）
- 已知设计取舍：变更采用"乐观更新 + 全量 bootstrap 刷新"，后续可优化为增量同步；接单大厅靠 15 秒轮询接近实时
- 完成后可随时把本清单中的勾选项同步到《木叶设计平台-开发文档.md》
