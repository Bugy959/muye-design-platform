# 木叶义齿设计平台 · 上线部署详细指南（腾讯云版）

> 版本：V1.0 | 更新时间：2026-08-28
> 适用：已确定 **腾讯云轻量应用服务器 + Ubuntu 22.04 + 腾讯云 COS + 免费 SSL** 的部署方案。
> 想快速勾选清单看《上线速查清单.md》；更细的 COS/备份/扩容深挖见《服务器部署详细指南.md》第 12 章与《服务器部署整改方案.md》。

---

## 0. 一图看懂部署结构

```
用户浏览器（HTTPS）
   │
   ▼
nginx（80/443，托管前端静态文件 + /api 反代）
   │              │
   │ /api/*       │ 前端静态（website/dist）
   ▼              ▼
Node 后端 :3001   浏览器直连 ↓ 大文件
（Express + SQLite）    ┌─────────────┐
   │                    │ 腾讯云 COS   │
   │ muye.db            │（口扫/设计稿）│
   ▼                    └─────────────┘
server/data/muye.db
```

- 大文件（口扫/设计稿）**不经过服务器**：浏览器拿 COS 预签名地址直接把文件传/下载到 COS。
- 服务器 3M 带宽只承载网页 + JSON 接口，600MB 文件不会被卡住。

---

## 1. 购买与前置准备

### 1.1 服务器
- 配置：**4核4G / 40GB SSD / 3Mbps / 300GB 流量**（够用，不用加配）。
- 镜像：**系统镜像 → Ubuntu 22.04 LTS（64 位）**。
  ⚠️ 别选「Node.js 22.12.0」应用镜像——后端需要 Node ≥ 22.13（内置 node:sqlite），22.12 起不来；也别选 WordPress/Dify/宝塔等预装栈。

### 1.2 域名 + ICP 备案（最慢，先启动）
1. 买域名（如 muye-design.com）。
2. 在**腾讯云备案系统**提交 ICP 备案（接入商=服务器所在云，必须一致）。需营业执照（个体户/公司）。
3. 备案期间可并行做 1.3/1.4 与服务器配置；备案通过前不要用域名直连 80/443 对外提供网站。

### 1.3 SSL 证书
- **不用买**。用 Let's Encrypt（certbot）免费证书自动续期，或腾讯云控制台免费 DV 证书（有效期一般 3 个月，记得自动续期）。

### 1.4 COS（大文件存储）
1. 腾讯云控制台 → 对象存储 COS → 创建存储桶：
   - 桶名：`muye-1250000000`（**必须 `名字-APPID` 格式**，APPID 在账号信息里）
   - 地域：`ap-shanghai`（离你近即可）
   - 权限：**私有读写（Private）**
2. 桶 → 安全管理 → CORS 规则：
   - AllowedOrigin：`https://你的域名.com`
   - AllowedMethod：`GET`、`PUT`
   - AllowedHeader：`*`
   - **ExposeHeaders：`ETag`**（分片合并必须读取分片 ETag，漏了会 403/失败）
3. 访问管理 CAM → 新建 **API 密钥**（SecretId / SecretKey），建议子账号只授该桶 `PutObject / GetObject / ListParts / CompleteMultipartUpload / AbortMultipartUpload / ListMultipartUploads` 权限，别用主账号密钥。

---

## 2. 登录服务器 & 基础环境

SSH 登录（控制台「一键登录」或 `ssh root@服务器IP`），然后：

```bash
sudo apt update && sudo apt install -y nginx sqlite3 git curl

# Node 24（用 nvm 装，避免系统源版本过旧）
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.1/install.sh | bash
# 重开终端后：
nvm install 24
nvm alias default 24

npm install -g pm2
```

- 验证：`node -v` 显示 v24.x；`node -e "console.log(typeof require('node:sqlite'))"` 能打印 `object` 即 node:sqlite 可用。
- 防火墙：控制台「防火墙」或系统 `ufw` 只放行 **80 / 443**（SSH 22 保留）。

---

## 3. 部署代码

```bash
git clone https://github.com/Bugy959/muye-design-platform.git /var/www/muye
cd /var/www/muye

# 前端构建（.env.production 已内置 VITE_API_BASE=/api）
cd website
npm ci
npm run build          # 产物在 website/dist

# 后端依赖
cd ../server
npm ci
```

---

## 4. 配置后端环境变量（pm2）

新建 `/var/www/muye/server/ecosystem.config.js`：

```js
module.exports = {
  apps: [{
    name: 'muye-server',
    cwd: '/var/www/muye/server',
    script: 'src/index.js',
    instances: 1,
    max_memory_restart: '800M',
    env: {
      PORT: 3001,
      NODE_ENV: 'production',
      // 跨域白名单（生产必设，与前端域名一致；同源 /api 其实不跨域，防御性设置）
      CORS_ORIGIN: 'https://你的域名.com',
      // nginx 反代一层 → 信任代理，登录限流按客户端真实 IP
      TRUST_PROXY: 1,
      // 腾讯云 COS（大文件直传/下载）
      COS_REGION: 'ap-shanghai',
      COS_BUCKET: 'muye-1250000000',
      COS_SECRET_ID: '你的SecretId',
      COS_SECRET_KEY: '你的SecretKey',
      // 可选：孤儿上传回收是否顺带删除 COS 对象（建议先开桶生命周期规则，不用设）
      // COS_ENABLE_CLEANUP: 'true',
    },
  }],
}
```

启动并开机自启：

```bash
cd /var/www/muye/server
pm2 start ecosystem.config.js
pm2 save
pm2 startup            # 按提示复制执行它输出的一行命令
pm2 status             # 应为 online
curl http://127.0.0.1:3001/api/health
# 预期：{"ok":true,"service":"muye-design-server","db":true,"cos":true,"time":"..."}
```

> 首次启动自动建库 `server/data/muye.db` 并写入演示账号，**上线前必须改/删**（见第 8 节安全清单）。

其他可用环境变量（默认值即可，不配也不用管）：

| 变量 | 默认 | 说明 |
|---|---|---|
| `LOGIN_MAX_PER_MINUTE` | 5 | 登录限流（次/分钟/IP） |
| `UPLOAD_MAX_PER_MINUTE` | 30 | 上传接口限流（次/分钟/账号） |
| `JSON_BODY_LIMIT` | 10mb | JSON 请求体上限（大文件走 COS，无需放大） |
| `DB_BUSY_TIMEOUT` | 5000 | SQLite 并发写等待毫秒 |
| `COS_ENABLE_CLEANUP` | 关 | 孤儿上传回收是否删 COS 对象（默认交给桶生命周期规则） |

---

## 5. nginx

新建 `/etc/nginx/sites-available/muye`：

```nginx
server {
    listen 80;
    server_name 你的域名.com;

    root /var/www/muye/website/dist;
    index index.html;

    # 大文件已走 COS，请求体用默认 1m 即可（无需 client_max_body_size 放大）
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 后端接口反代
    location /api/ {
        proxy_pass http://127.0.0.1:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

启用并测试：

```bash
sudo ln -s /etc/nginx/sites-available/muye /etc/nginx/sites-enabled/
sudo nginx -t && sudo systemctl reload nginx
```

---

## 6. HTTPS（免费证书）

```bash
sudo apt install -y certbot python3-certbot-nginx
sudo certbot --nginx -d 你的域名.com
# 按提示输入邮箱；选择「重定向 HTTP → HTTPS」
# certbot 会自动改 nginx 配置并开启 90 天自动续期
```

验证自动续期：`sudo certbot renew --dry-run`

完成后访问 `https://你的域名.com` 应看到登录页。

---

## 7. 每日自动备份（务必做）

整个平台的数据只有一个文件 `server/data/muye.db`。数据库开了 WAL 模式，**不要用 cp 直接拷**（可能拷到未合并事务），用 SQLite 在线备份：

```bash
mkdir -p /var/backups/muye
sudo tee /etc/cron.daily/muye-backup >/dev/null <<'EOF'
#!/bin/sh
# WAL 模式下用 VACUUM INTO 在线一致性备份（无需停服）
sqlite3 /var/www/muye/server/data/muye.db "VACUUM INTO '/var/backups/muye/muye-$(date +%F).db'"
find /var/backups/muye -name 'muye-*.db' -mtime +30 -delete
EOF
sudo chmod +x /etc/cron.daily/muye-backup
```

> 恢复：把备份文件拷回 `server/data/muye.db` 重启 pm2 即可。建议每月把备份下载到本地一份，并做一次恢复演练（用备份能正常启动）。

---

## 8. 上线前安全清单

- [ ] 改掉/删除演示账号：admin（muye2026）、mingzhou/hengmei/yahe（123456）、li/wang/zhao/sun/zhou（123456）
- [ ] `CORS_ORIGIN` 已指向正式域名
- [ ] 防火墙只开 80/443（3001 不对外）
- [ ] COS 桶私有 + 密钥已用子账号最小权限
- [ ] 每日备份 cron 生效（`ls /var/backups/muye/` 能看到今天的文件）
- [ ] HTTPS 已生效（浏览器有小锁）

---

## 9. 上线验证（按顺序）

- [ ] `curl https://你的域名.com/api/health` → `{"ok":true,"db":true,"cos":true,...}`
- [ ] 网页打开、能登录（admin 用临时强密码）
- [ ] 医院端：下单并**实测上传口扫/照片** → 订单出现、文件大小与进度条正常
- [ ] 设计师端：接单大厅可见订单 → 接单 → 交稿（上传设计文件）
- [ ] 医院端：能看到「已完成」消息并**下载设计文件**
- [ ] 管理端：积分调整、分组、账号创建各点一遍
- [ ] 手机切 4G 访问一次（确认不是只在服务器本机好使）

---

## 10. 日常维护与更新

```bash
# 更新版本
cd /var/www/muye
git pull
cd website && npm ci && npm run build
cd ../server && npm install
pm2 restart muye-server
sudo systemctl reload nginx

# 看日志
pm2 logs muye-server
sudo tail -f /var/log/nginx/error.log

# 看 COS 费用（别让它悄悄扣费）
# 云控制台 → 费用中心 → 账单，设阈值告警
```

## 常见问题（FAQ）

**Q1：页面打开但一直“连不上服务器/请登录”？**
大概率前端没走 `/api`。确认：① 构建用了已提交的 `.env.production`（`VITE_API_BASE=/api`）；② `curl https://域名/api/health` 通；③ nginx `location /api/` 生效（改配置后 `nginx -t && systemctl reload nginx`）。

**Q2：上传口扫文件报错/413？**
大文件走 COS 不经过服务器。若报 413：检查是不是误把 nginx `client_max_body_size` 调小（默认 1m 即可，不要设成小值）；后端 `JSON_BODY_LIMIT` 默认 10mb 也够。若报 403：多半是 COS CORS 的 **ExposeHeaders 缺 `ETag`**。

**Q3：下载文件转圈/403？**
前端点下载会调 `/files/download-url` 实时签名，不应过期。若 403：确认订单文件 key 确实属于当前账号（去 COS 桶里搜该 key 是否存在），以及 COS 地域/桶名环境变量是否正确。

**Q4：Node 版本问题？**
要求 Node ≥ 22.13。用 nvm 装 24 即可；不要用 Ubuntu 自带源（可能装出 18/20）。

**Q5：3001 端口被占用/起不来？**
`pm2 status` 看是不是重复起了实例；或换 `PORT=3002` 并同步改 nginx `proxy_pass`。