# 木叶设计平台 —— 服务器部署详细指南（含前后端修改方案）

> 适用人群：对代码不熟悉、要自己把平台部署到一台公网服务器的人。
> 本文是"实操版"：每一步给命令、给预期结果、给需要改的代码（改动前后对照）。
> 配套文档：《服务器部署整改方案.md》（上线大项规划：COS / 备案 / 备份路线）、`server/README.md`（后端 API 说明）。

---

## 0. 一分钟看懂整体方案

```
用户浏览器
   │  https://你的域名.com
   ▼
nginx（80/443，负责 HTTPS 和转发）
   ├── /        → 前端静态文件（website/dist 构建产物）
   └── /api/*   → 转发给后端 http://127.0.0.1:3001
                        ▼
                Express 后端（server）
                        ▼
                SQLite 数据库（server/data/muye.db，唯一数据文件）
```

- **前端**：Vite + React + TypeScript，`npm run build` 后会生成一堆静态文件（html/js/css），让 nginx 直接托管即可。
- **后端**：Node.js + Express，占用 **3001 端口**，负责所有业务（账号/订单/积分/消息），数据存 **SQLite 单文件**。
- **为什么用 nginx 转发而不是直接暴露 3001**：统一走 80/443、能上 HTTPS、隐藏后端端口更安全、以后加反向代理缓存也方便。

---

## 1. 部署形态：三种方案对比

| 方案 | 形态 | 难度 | 适用 |
|---|---|---|---|
| **A（推荐）** | nginx 托管前端静态文件 + `/api` 反代后端 | ★★ | 正式上线，用这个 |
| B | 前端 `npx serve dist` 占 8080，后端裸跑 3001，直接开放两个端口 | ★ | 快速演示/内网试跑 |
| C | Docker 打包前后端 | ★★★★ | 以后要弹性扩容再说 |

本文按 **方案 A** 写主流程；B 只给要点（见第 3.3 节）。

---

## 2. 前端修改方案（部署前必须确认）

### 2.1 接口地址怎么定（最重要）

前端调后端的地址由 `website/src/lib/api.ts` 决定：

```ts
API_BASE = 环境变量 VITE_API_BASE || 默认值(http://当前域名:3001/api)
```

也就是说：
- **默认行为**：浏览器访问 `http://服务器IP:3000` 时，前端会去连 `http://服务器IP:3001/api`（跨端口=跨域，浏览器会拦）。**这就是本地开发的方式，但正式部署不建议直接用。**
- **推荐做法（同源）**：构建时指定 `VITE_API_BASE=/api`，前端请求变成 `/api/xxx`，由 nginx 转发给 3001，不存在跨域问题。

**具体操作（二选一）：**

方式一：构建命令带环境变量（临时、不落库）
```bash
cd /var/www/muye/website
VITE_API_BASE=/api npm run build
```

方式二（推荐，固化到仓库）：`website/.env.production` 已在本仓库创建并提交，内容：
```ini
# website/.env.production
VITE_API_BASE=/api
```
之后在服务器/CI 上直接 `npm run build` 即可，不需要每次输环境变量。

> 说明：`VITE_` 开头的变量会被 Vite 构建时写死进打包产物，**改这个必须重新 build**，改 .env 不重 build 不生效。

### 2.2 构建命令与产物

```bash
cd website
npm install        # 首次装依赖
npm run build      # = tsc -b（类型检查）+ vite build
# 产物目录：website/dist/（index.html + assets/...）
```

- `vite.config.ts` 里 `base: './'` 已配置成相对路径，前端资源可以放在域名根目录或任意子目录，都不影响。
- 构建失败通常是因为 TypeScript 类型错误，看报错改代码后重跑。

### 2.3 必须知道的"数据模式"开关

前端有两套数据源：
- **后端模式（正式用）**：所有数据走后端 API。
- **演示模式**：数据存浏览器 localStorage，后端只用来登录不了。

判断逻辑在 `api.ts` 的 `isBackendMode()`：`localStorage` 里出现 `muye-data-mode = demo` 就进入演示模式。
**正式环境绝对不要设置这个值**（比如在浏览器控制台 `localStorage.setItem('muye-data-mode','demo')`），否则看到的是浏览器本地假数据。

---

## 3. 后端修改方案（部署前必须修改）

### 3.1 收紧 CORS（必做，安全）

当前 `server/src/index.js` 第 14~20 行是开发用中间件，允许任意来源访问：

```js
// 修改前（开发期）：任意来源都放行
app.use((req, res, next) => {
  res.setHeader('Access-Control-Allow-Origin', req.headers.origin || '*')
  res.setHeader('Access-Control-Allow-Headers', 'Content-Type, Authorization')
  res.setHeader('Access-Control-Allow-Methods', 'GET,POST,PATCH,DELETE,OPTIONS')
  if (req.method === 'OPTIONS') return res.sendStatus(204)
  next()
})
```

**修改后（正式环境）——代码已完成（2026-08-25），只需配环境变量：**

后端已支持 `CORS_ORIGIN` 环境变量收紧白名单（未配置时保持开发期放行，便于本地调试）：

```bash
# pm2 ecosystem.config.cjs 或启动命令里设置：
CORS_ORIGIN=https://你的域名.com
```

配置后 `Access-Control-Allow-Origin` 只返回该域名。

> 如果用了"方案 A 同源 /api"，浏览器同源请求本身不触发 CORS，这条更多是防御性设置（防止别人直接跨域调你的 3001）。

### 3.2 端口、数据库路径、环境变量说明

| 环境变量 | 默认值 | 作用 |
|---|---|---|
| `PORT` | 3001 | 后端监听端口。改了就同时改 nginx 的 `proxy_pass` |
| `MUYE_DB_PATH` | `server/data/muye.db` | SQLite 数据库文件位置。想放到独立数据盘/挂载卷时用它 |
| `NODE_ENV` | （不设置） | **别设成 `test`**！`index.js` 里 `NODE_ENV !== 'test'` 才会启动监听，设成 test 后端会"静默不启动" |
| `CORS_ORIGIN` | 不设置=开发期放行 | 跨域白名单：设为正式域名后 `Access-Control-Allow-Origin` 只返回该域名（3.1） |
| `LOGIN_MAX_PER_MINUTE` | 5 | 登录限流：每 IP 每分钟允许的登录尝试次数（防暴力破解） |
| `TRUST_PROXY` | 1 | nginx 反代层数（按 X-Forwarded-For 取真实 IP 做限流） |
| `JSON_BODY_LIMIT` | 10mb | JSON 请求体上限（大文件已走 COS，无需放大） |
| `DB_BUSY_TIMEOUT` | 5000 | SQLite 并发写等待毫秒（WAL 下避免偶发 SQLITE_BUSY） |
| `COS_ENABLE_CLEANUP` | 关 | 孤儿上传回收是否顺带删除 COS 对象（默认交给桶生命周期规则） |
| `UPLOAD_MAX_PER_MINUTE` | 30 | 上传接口限流：每账号每分钟 upload-token/upload-init 次数上限（防恶意刷 COS） |

### 3.3 演示账号必须在正式环境处理

首次启动自动建库并写入 9 个演示账号（密码全是弱的）：

| 账号 | 密码 | 角色 |
|---|---|---|
| admin | muye2026 | 管理端 |
| mingzhou / hengmei / yahe | 123456 | 医院端 |
| li / wang / zhao / sun / zhou | 123456 | 设计师端 |

处理方式（至少做一项）：
1. 上线后登录对应账号，在界面里把密码改成强密码；
2. 不需要的演示医院/设计师账号，由 admin 在管理端删除；
3. 如果清库重来，可以停后端 → 看《服务器部署整改方案.md》里清库方式 → 重启。

### 3.4 大文件说明（口扫/照片，重要）

**现状（2026-08-28 起）：大文件走腾讯云 COS 直传（代码就绪，待配置环境变量）。**
- ≤1.5MB 小文件仍以 dataUrl 内嵌在 JSON 里传输（base64 后约 2MB），后端 `express.json({ limit: '10mb' })` 已足够。
- >1.5MB 大文件在后端模式走 COS 直传（≤50MB 单次、>50MB 分片断点续传），**不经过服务器**，nginx 请求体上限保持默认 1m 即可。
- 完整步骤见本章第 12 节与《服务器部署整改方案.md》。

### 3.5 后端健康检查与启动验证

```bash
curl http://localhost:3001/api/health
# 预期：{"ok":true,"service":"muye-design-server","db":true,"cos":false,"time":"..."}（db=数据库探活，cos=是否已配置腾讯云 COS）
```

---

## 4. 服务器环境准备（一次性）

```bash
# 更新系统
sudo apt update && sudo apt upgrade -y

# 安装 Node.js 22 LTS（后端要求 >=22.13；Vite 7 也要求 22.12+；本仓库已锁定 engines 并提供 `.nvmrc`=24，本地用 `nvm use` 一键切换）
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
sudo apt-get install -y nodejs git nginx

# 安装 pm2（后端进程守护：崩溃自动重启 + 开机自启）
sudo npm install -g pm2

# 验证
node -v        # 应显示 v22.x（≥ v22.13）
npm -v
nginx -v
pm2 -v
```

---

## 5. 部署步骤（方案 A 主流程）

### 5.1 拉代码 + 装依赖

```bash
sudo mkdir -p /var/www
cd /var/www
sudo git clone https://github.com/Bugy959/muye-design-platform.git muye
sudo chown -R $USER:$USER /var/www/muye   # 让当前用户可写，后面省事

cd /var/www/muye/server
npm install

cd /var/www/muye/website
npm install
```

### 5.2 按"第 2、3 节"改代码

- [ ] 前端：建好 `website/.env.production`（内容 `VITE_API_BASE=/api`）
- [ ] 后端：CORS 支持 `CORS_ORIGIN` 环境变量（3.1，代码已就绪），部署时给 pm2/ecosystem 配正式域名

### 5.3 构建前端

```bash
cd /var/www/muye/website
npm run build
ls dist/        # 应该能看到 index.html 和 assets/
```

### 5.4 启动并守护后端

```bash
cd /var/www/muye/server

# 首次：直接前台跑一次，确认能起、能打出种子数据日志
npm start
# 看到 "[muye] 后端服务已启动: http://localhost:3001"
# 再开一个终端验证：
curl http://localhost:3001/api/health

# 确认没问题后 Ctrl+C 停掉，改用 pm2 常驻
pm2 start npm --name muye-server -- start
pm2 save                     # 保存进程列表
pm2 startup                  # 生成开机自启命令，复制它输出的那一行再执行一遍
pm2 status                   # online = 成功
```

pm2 常用命令：
```bash
pm2 logs muye-server     # 看后端日志
pm2 restart muye-server  # 重启
pm2 stop muye-server     # 停止
```

### 5.5 配置 nginx

新建 `/etc/nginx/sites-available/muye`：

```nginx
server {
    listen 80;
    server_name 你的域名.com;

    # 前端静态文件
    root /var/www/muye/website/dist;
    index index.html;

    # 大文件已走 COS 直传（2026-08-28 起）：请求体只需承载 ≤2MB 的 dataUrl 小文件，
    # nginx 默认 1m 即可，无需调大；如需放宽后端限制可用 JSON_BODY_LIMIT 覆盖

    # 单页应用：找不到文件回退到 index.html（前端路由刷新不 404）
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 后端接口转发
    location /api/ {
        proxy_pass http://127.0.0.1:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 120s;   # 大文件上传/下载给足时间
    }
}
```

启用：

```bash
sudo ln -s /etc/nginx/sites-available/muye /etc/nginx/sites-enabled/
sudo nginx -t                 # 显示 syntax is ok 才继续
sudo systemctl reload nginx
sudo rm -f /etc/nginx/sites-enabled/default   # 如果有默认站点，删掉避免抢 80 端口
sudo systemctl reload nginx
```

### 5.6 配置 HTTPS（正式上线必做）

```bash
sudo apt-get install -y certbot python3-certbot-nginx
sudo certbot --nginx -d 你的域名.com
# 按提示填邮箱，最后选择 "2"（把 HTTP 重定向到 HTTPS）
# certbot 会自动改 nginx 配置并创建自动续期定时任务

# 手动验证自动续期是否配置好
sudo certbot renew --dry-run
```

---

## 6. 数据与备份（务必做）

**整个平台的数据只有一个文件：`server/data/muye.db`。** 数据库里存了：账号、客户/设计师档案、订单、积分流水、消息、分组匹配、设计参数。

### 每日自动备份

```bash
mkdir -p /var/backups/muye
sudo tee /etc/cron.daily/muye-backup >/dev/null <<'EOF'
#!/bin/sh
# WAL 模式下直接 cp 可能拷到未合并的事务，改用 SQLite 在线导出 VACUUM INTO（无需停服）
# 首次使用先装 CLI：sudo apt-get install -y sqlite3
sqlite3 /var/www/muye/server/data/muye.db "VACUUM INTO '/var/backups/muye/muye-$(date +%F).db'"
find /var/backups/muye -name 'muye-*.db' -mtime +30 -delete
EOF
sudo chmod +x /etc/cron.daily/muye-backup
```

> `VACUUM INTO` 是 SQLite 内置的在线一致性备份，无需停服；要求服务器装有 `sqlite3` CLI。恢复时把备份文件拷回 `server/data/muye.db` 后重启后端即可。建议每月把备份文件下载到本地电脑一份，并做一次恢复演练。

### 恢复数据库

```bash
pm2 stop muye-server
cp /var/backups/muye/muye-2026-08-23.db /var/www/muye/server/data/muye.db
pm2 start muye-server
```

> 强烈建议：**每月手动把一份备份下载到本地电脑/U盘**，服务器挂了也不怕。

---

## 7. 安全清单（上线前逐项打勾）

- [ ] 改掉/删除全部演示账号（admin/muye2026、医院 123456、设计师 123456）
- [ ] 后端 CORS 已收紧（第 3.1 节），`CORS_ORIGIN` 指向正式域名
- [ ] 防火墙只开 80/443（如果用方案 A）：
      ```bash
      sudo ufw allow 80,443/tcp
      sudo ufw enable
      sudo ufw status
      ```
      3001 不必对外开放（走 nginx 内部转发）
- [ ] 服务器 SSH 用密钥登录并禁用密码登录（可选项，强烈建议）
- [ ] HTTPS 已生效（浏览器地址栏有小锁）
- [ ] 数据库每日备份 cron 已生效（`ls /var/backups/muye/` 能看到今天的文件）
- [ ] `NODE_ENV` 未被设置成 `test`
- [ ] 大文件对象存储（COS）——代码已就绪（2026-08-28 腾讯云 COS 分片直传 + 断点续传），配置环境变量并验证直传（第 12 章）

---

## 8. 监控 / 日志 / 日常维护

| 需求 | 命令/位置 |
|---|---|
| 后端是否活着 | `pm2 status`、`curl http://localhost:3001/api/health` |
| 后端日志 | `pm2 logs muye-server`（`/root/.pm2/logs/` 有文件） |
| nginx 访问日志 | `/var/log/nginx/access.log` |
| nginx 错误日志 | `/var/log/nginx/error.log` |
| 磁盘/内存 | `free -h`、`df -h` |
| 定时健康检查（可选） | cron 每小时 curl health，失败发邮件/重启用 `pm2 restart` |

省心做法：给服务器配一个免费的监控（如 UptimeRobot，免费版可盯 1 个 URL），挂了立刻邮件提醒。

---

## 9. 更新版本 & 回滚

### 更新到新版本

```bash
cd /var/www/muye
git pull                                # 拉最新代码
git status                              # 确认没有本地改动冲突

# 前端
cd website && npm install               # 有新依赖才需要
npm run build                           # 重新构建（VITE_API_BASE 已在 .env.production 里）

# 后端
cd ../server && npm install             # 有新依赖才需要
pm2 restart muye-server

# 数据库结构若变了：暂无自动迁移脚本，谨慎——先备份 db 再启动，看报错
sudo systemctl reload nginx             # 保险起见
```

### 回滚

- **前端回滚**：没有数据库风险，直接 `git checkout <上一个提交> -- website` 重 build，或保留旧 `dist` 覆盖回去。
- **后端回滚**：
  ```bash
  cd /var/www/muye/server
  git log --oneline -5                  # 找到上一个好的 commit
  git reset --hard <上个commit>          # ⚠️ 会丢弃这个目录的本地改动，先确认
  pm2 restart muye-server
  ```
- **数据回滚**：把备份的 `muye.db` 拷回去重启（见第 6 节）。**升级前一定先备份数据库**。

---

## 10. 常见问题排查（FAQ）

### Q1 页面打开了但提示"连不上服务器 / 登录失败"
排查顺序：
1. `curl https://你的域名.com/api/health` —— 不通则看 nginx 和 3001
2. `pm2 status` 确认后端 online
3. 确认构建时用了 `VITE_API_BASE=/api`（第 2.1 节）
4. `sudo tail -50 /var/log/nginx/error.log`

### Q2 上传口扫文件失败/报错
大文件已走 COS 直传（第 12 章）。小文件 dataUrl（≤1.5MB）在后端 10mb 限制内。若仍报"请求体过大/413"，检查后端 `JSON_BODY_LIMIT` 是否被误调小，以及 nginx 的 `client_max_body_size`（保持默认 1m 即可）。

### Q3 端口 3001 被占用 / 起不来
`ss -ltnp | grep 3001` 看谁占着；或换 `PORT=3002 pm2 start ...` 并同步改 nginx `proxy_pass`。

### Q4 后端启动后立刻退出 / 没有日志
`pm2 logs muye-server` 看报错。常见原因：
- Node 版本 < 22.13（没有 `node:sqlite`）→ 升级 Node
- `NODE_ENV=test` → 不会启动监听（第 3.2 节）
- `server/data` 目录没有写权限 → `chmod -R u+rwX data`

### Q5 换服务器 / 迁移数据
1. 旧服务器停后端 `pm2 stop muye-server`，拷走 `server/data/muye.db`
2. 新服务器按本文装环境、拉代码
3. 用 `MUYE_DB_PATH` 或直接把 db 放到 `server/data/`，启动即用

### Q6 改密码后另一个设备还能登录吗
不能。后端改密/删号会立刻作废该账号的所有 token（会话 7 天有效，改密强制重新登录）。

---

## 11. 上线验证清单（最终检查）

- [ ] `https://你的域名.com/api/health` 返回 `{"ok":true,...}`
- [ ] 浏览器打开域名，地址栏有小锁（HTTPS）
- [ ] 用 admin 登录，改掉初始密码
- [ ] 走一遍完整业务闭环：
      - 医院端提交订单（含上传一张口扫文件）
      - 设计师端在接单大厅看到并接单
      - 设计师提交设计文件 → 医院端收到"已完成"消息
      - 医院申请一次返工 → 管理端审核通过/拒绝 → 积分流水正确
- [ ] 管理端：积分调整、创建医院/设计师账号、分组管理、匹配规则都能用
- [ ] 手机 4G 访问一遍（排除"只有本机好使"）
- [ ] 确认 `/var/backups/muye/` 今天有备份文件
- [ ] 关闭浏览器再开、换一台设备登录，数据一致（确认用的是后端数据而非演示模式）

---


## 12. 1GB 大文件处理方案（推荐：COS 上云，别让文件碰服务器）

> 对应你最终选的机器：4核4G / 40GB SSD / 3Mbps / 300GB 流量。
> 结论：**文件不要存这台服务器**，全部走对象存储（腾讯云 COS），这台机器只当"业务大脑"。

### 12.1 一句话方案

- 医院浏览器选好文件 → 向后端要一个"直传凭证" → **直接把文件传到 COS**（不经过你的服务器，不占你的带宽/磁盘/流量）。
- 数据库里订单只记文件的"钥匙"（key），不存文件内容。
- 下载时后端把 key 换成"短期签名下载地址"，设计师点开就能下。

### 12.2 为什么必须这样（对照你这台机器）

| 痛点 | 不解决会怎样 | 走 COS 后 |
|---|---|---|
| 3Mbps 带宽 | 500MB 传一次 22-27 分钟，下载也这么慢 | 上传走 COS 高带宽，几十秒~1 分钟 |
| 40GB 磁盘 | 一周就被文件塞满 | 服务器 0 文件占用 |
| 300GB 流量 | 大文件走服务器，流量/带宽都扛不住 | 下载走 COS/CDN，不占服务器流量 |

### 12.3 你要去云厂商做的事（控制台，约 30 分钟）

1. 开通腾讯云 COS，新建存储桶（Bucket），权限选**私有读写（Private）**（桶名建议 `muye-<你的APPID数字>`，如 `muye-1250000000`）
2. 记下：Region（地域）、Bucket 名称
3. 创建 AccessKey（强烈建议只给"这个桶"的受限权限，别用主账号 Key）——用 RAM 子账号只授该私有桶的最小权限：
   - 只授 `PutObject` / `GetObject` / `ListParts` / `CompleteMultipartUpload` / `AbortMultipartUpload` / `ListMultipartUploads`（单次直传 + 分片直传 + 下载所需）；
   - 权限范围限定到该桶（`Resource` 写 `<桶名>` 与 `<桶名>/*`），不要授全账号权限，更不要用主账号 AccessKey。
4. 给桶配 **CORS**（否则浏览器直传会被拦）：
   - AllowedOrigin：你的正式域名（如 `https://你的域名.com`）
   - AllowedMethod：`GET`、`PUT`
   - AllowedHeader：`*`
   - ExposeHeader：`ETag`
   - （分片上传需读取每个分片的 ETag，ExposeHeader **必须包含** `ETag`）
5. （可选）绑 CDN 加速下载

### 12.4 改后的数据流

1. 医院选好文件 → 前端调用 `POST /api/files/upload-token`（带 `{name, size}`）
2. 后端用 AccessKey 生成"预签名 PUT 地址"（默认 1 小时有效）+ 随机文件 key，返回给前端
3. 前端把文件直接 `PUT` 到该地址（**不经服务器**）
4. 前端把 key 写进订单（`scan_files` / `images` 元素变成 `{name, key, size}`）
5. 后端查订单时，把 key 换成"短期签名下载地址"，设计师下载/看图

> **代码状态：已落地本仓库（2026-08-28 起为腾讯云 COS）。** 后端 `server/src/files.js`、`/files/upload-token` 等接口，以及前端 `api.ts` / `store.ts` / `types/index.ts` 的相关改动都已写好并通过测试；你只差在腾讯云开通 COS、配好环境变量。

### 12.5 后端代码改动（server）

安装依赖：

```bash
cd server && npm install cos-nodejs-sdk-v5
```

环境变量（加到 pm2 启动配置或 `.env`）：

```ini
COS_REGION=ap-shanghai
COS_BUCKET=你的桶名-1250000000
COS_SECRET_ID=你的SecretId
COS_SECRET_KEY=你的SecretKey
```

// server/src/files.js —— 腾讯云 COS 预签名直传/下载（完整实现以仓库代码为准）
import COS from 'cos-nodejs-sdk-v5'
```

**实现要点（2026-08-28 起为腾讯云 COS）**：
- 签名统一用 SDK `getObjectUrl`（本地 HMAC 签名，不产生网络请求），返回带 `q-sign-*` 的直传/下载地址；相关函数均为**异步**，路由里用 `await` 调用。
- 单次直传：`getUploadTarget(filename, size)` → `{ key, uploadUrl }`（PUT，有效期 1 小时）。
- 分片直传：`initMultipartUpload` → `getUploadPartUrl(key, uploadId, partNumber)`（签名 URL 带 `partNumber`/`uploadId`）→ `completeMultipartUpload(key, uploadId, parts)`（映射为 COS 的 `PartNumber`/`ETag`）。
- 下载：`getDownloadUrl(key)`（GET，有效期 2 小时）。
- 桶名必须是 `名字-1250000000` 格式；Bucket 权限**私有读写**；COS CORS 的 ExposeHeaders 需包含 `ETag`（分片合并要读分片 ETag）。

> **2026-08-24 升级**：预签名有效期已从 10 分钟调至 **1 小时**；大文件（>50MB）自动走**分片直传**：`POST /api/files/upload-init` → `POST /api/files/upload-part-url` → `POST /api/files/upload-complete`，浏览器逐片上传（5MB/片、并发 4），失败/刷新可**断点续传**（分片接口见 `server/src/routes.js`，前端实现见 `website/src/lib/store.ts` 的 `uploadMultipart`）。

在 `server/src/routes.js` 挂两个东西（顶部 import 之后）：

```js
import { getUploadTarget, getDownloadUrl } from './files.js'

// 任意已登录角色申请上传凭证（医院传口扫/照片、设计师传设计稿）
routes.post('/files/upload-token', requireAuth, h(async (req, res) => {
  const { name, size } = req.body || {}
  if (!name || !Number.isInteger(size) || size <= 0) return bad(res, '请提供文件名和大小')
  const { key, uploadUrl } = await getUploadTarget(name, size)
  res.json({ key, uploadUrl })
}))

// 把订单里的文件 key 换成签名下载地址（bootstrap 返回订单前调用，异步）
async function withSignedFiles(o) {
  const sign = async (list) => {
    const out = []
    for (const f of list || []) out.push(f.key ? { ...f, url: await getDownloadUrl(f.key) } : f)
    return out
  }
  return { ...o, scanFiles: await sign(o.scanFiles), images: await sign(o.images), designFiles: await sign(o.designFiles) }
}
```

> 说明：数据库表结构**不用改**（`scan_files` / `images` 仍是 JSON 数组），只是元素从 `{name, dataUrl}` 变成 `{name, key, size}`。`express.json({ limit: '10mb' })`（2026-08-25 起默认），大文件不从这里走。

### 12.6 前端代码改动（website）

`website/src/lib/api.ts` 新增：

```ts
/** 向后端申请 COS 直传地址 */
export const apiGetUploadToken = (token: string, name: string, size: number) =>
  apiFetch<{ key: string; uploadUrl: string }>('/files/upload-token', { method: 'POST', token, body: { name, size } })

/** 把文件直接 PUT 到 COS（不经过后端） */
export async function apiUploadFile(uploadUrl: string, file: File): Promise<void> {
  const response = await fetch(uploadUrl, {
    method: 'PUT',
    headers: { 'Content-Type': 'application/octet-stream' },
    body: file,
  })
  if (!response.ok) throw new Error(`文件上传失败（${response.status}）`)
}
```

`website/src/lib/store.ts` 里把"读文件"改成"先上传再记 key"：

```ts
/** 上传口扫/照片：返回订单要保存的文件对象 */
export async function uploadOrderFile(file: File): Promise<OrderFile> {
  const { key, uploadUrl } = await apiGetUploadToken(sessionToken(), file.name, file.size)
  await apiUploadFile(uploadUrl, file)
  return { name: file.name, key, size: file.size }
}
```

- 创建订单时，`scanFiles` / `images` 用 `uploadOrderFile` 返回的值；
- 下载/预览用订单里的签名 `url`（后端已处理）。
- ≤50MB 单次直传；>50MB 分片直传（5MB/片、并发 4、自动断点续传）。`uploadOrderFile(file, onProgress)` 可回调上传进度。
- 分片 PUT 失败自动重试 3 次（指数退避）；complete 阶段 uploadId 失效自动清检查点、重新 init 整传一次（`store.ts uploadMultipart`）。
- 下载/预览实时签名：点按钮时调 `POST /api/files/download-url { key }`（后端按角色校验归属）拿新地址再打开，页面挂久不再 403。
- 界面：下单表单扫描文件/照片上传时显示**进度条**（`readOrderFile(file, onProgress)` 透传百分比）；`apiFetch` 内置 20s 超时（`fetchWithTimeout`），COS 直传不受影响。

### 12.7 nginx 改回去（重要）

- 文件不经过服务器：若你之前手动加过 `client_max_body_size` 放大指令，现在删掉或改回默认（1m）——文档 5.5 已不再配置放大；
- 如果你之前按"自托管方案"改过 nginx `proxy_request_buffering off;`，同样可以还原——平台其它接口都是小请求。

### 12.8 没上 COS 前的过渡（可选）

想先跑通再切 COS？两步走：
1. 先做"自托管版"：用 multer 把文件存服务器磁盘，前端同样先调"上传接口"拿地址，订单只存地址；
2. 代码里把上传/取地址抽成两个函数（如 `storeFile()` / `resolveFileUrl()`），COS 就绪后只换实现、前端不用大改。

### 12.9 大概成本（以官网实时价为准）

| 项目 | 量级（参考） |
|---|---|
| COS 存储费 | 约 0.10-0.15 元/GB/月 |
| COS 下行流量 | 约 0.4-0.5 元/GB（直连） |
| CDN 下行流量 | 更便宜，且下载更快 |
| 服务器 | 维持 4C4G / 40G / 3M 不用升级 |

> 对比：文件硬塞服务器 = 40G 盘一周满 + 3Mbps 传一次 20 多分钟。COS 用多少付多少，是 1GB 文件场景最省钱的方案。

### 12.10 上线顺序与验收

- [ ] 开通 COS，私有桶 + CORS 配好（ExposeHeaders 含 `ETag`）
- [ ] 服务器加 COS 环境变量（pm2 配置里）
- [ ] 后端 `files.js` + `/files/upload-token` 跑通：curl 能拿到 `uploadUrl`
- [ ] 前端改造上传与订单（12.6 节）
- [ ] 用真实 500MB-1GB 口扫文件走一遍全流程：上传 → 下单 → 设计师下载 → 看图
- [ ] 验证安全性：不登录/没签名打不开文件；文件 key 随机不可猜
- [ ] 开启账单预警（COS 有量有费用，别让它悄悄扣费）

## 附：部署进度速查表

| 阶段 | 关键动作 | 完成（打勾） |
|---|---|---|
| 代码 | 推送 main，CI 全绿 | ☐ |
| 前端修改 | `.env.production` 写 `VITE_API_BASE=/api` | ☐ |
| 后端修改 | CORS 收紧 | ☐ |
| 环境 | Node ≥22.13 + nginx + pm2 | ☐ |
| 构建 | `website` 下 `npm run build` 成功 | ☐ |
| 启动 | pm2 启动后端，health 通过 | ☐ |
| 网关 | nginx 静态 + /api 反代（请求体保持默认 1m） | ☐ |
| 安全 | HTTPS、改演示密码、防火墙、备份 cron | ☐ |
| 大文件 | COS 开通 + 直传打通（第 12 章） | ☐ |
| 验收 | 第 11 节清单全过 | ☐ |