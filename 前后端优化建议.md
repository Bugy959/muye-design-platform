# 木叶设计平台 · 前后端修改方案优化建议

> 版本：V1.0 | 日期：2026-08-25
> 依据：通读全仓 20 份 Markdown 文档 + 后端 `server/src`（routes.js / files.js / index.js / db.js / auth.js / schema.sql）+ 前端 `website/src`（api.ts / store.ts / App.tsx）后的优化建议。
> 建议定位：本文是「下一轮迭代的改进清单」，不是立即全部执行；按 P0 → P1 → P2 顺序推进，P0 必须在上线前完成。
>
> **进度（2026-08-25）**：第一轮 P0 已提交并推送；第二轮 P1（1.7-1.9、1.11-1.14 与 2.5、2.7）已提交并推送；第三轮 A 套餐（健康检查探活 / 上传限流 / Node 版本锁定 / 2.4 暂存提示 / 2.6 前端 1GB 预校验）已完成。详见《交接文档》第十五~十八节。剩余：P2 各项（增量同步 1.10+2.9、routes/store 拆分 2.10、zod 校验、组件级测试 2.8、分页 2.11）。

---

## 〇、通读结论：文档体检

现有文档体系已经相当完整（开发文档 / 交接文档 / 部署指南 / 部署整改方案 / 测试说明 / 使用说明 / 质量清单），路线清楚。但通读后发现几个需要修正的「文档滞后」问题：

| # | 问题 | 位置 | 建议 |
|---|---|---|---|
| 1 | 测试数量前后对不上（测试说明写 21/90，交接文档写 25/100，质量清单还写 71/14） | 测试说明与部署清单.md、交接文档、前端改造与质量清单.md | 以最后一次全绿结果为准统一口径，并加一行「更新时间」 |
| 2 | 开发文档「十七、大文件上传」只写了单次直传（upload-token），没有 8-24 新增的分片接口 | 木叶设计平台-开发文档.md | 补上 upload-init / upload-part-url / upload-complete 三接口与断点续传说明 |
| 3 | 部署清单里 nginx 还保留 `client_max_body_size 110m`，与 OSS 方案冲突 | 测试说明与部署清单.md（第 7 步安全清单仍写「后端口子放宽到 100MB」） | 统一改为「大文件已走 OSS，json 限制调回小值」 |
| 4 | 备份方案直接 `cp muye.db`，在 WAL 模式下可能拷到不完整数据 | 测试说明与部署清单.md（第 8 步）、服务器部署详细指南.md | 改用 `VACUUM INTO` / sqlite3 `.backup`，见下文 1.5 |
| 5 | 服务器配置前后出现「2核2G」和「4核4G」两个版本 | 部署整改方案 vs 部署详细指南 | 定稿为 4核4G/40G SSD/3M/300G 流量（文件已上 OSS，这个规格够用） |
| 6 | 交接文档第 13/15 条与开发文档的 V2.9 时间戳不一致 | 交接文档（8-21）已含 8-24 记录 | 版本号统一升到 V2.10 或单独列变更，避免版本混用 |

---

---

## 一、后端优化建议（按优先级）

### P0 · 上线前必须做（安全与正确性）

**1.1 CORS 收紧为域名白名单**
现状：`server/src/index.js` 里 `Access-Control-Allow-Origin` 取请求头 origin（任意来源都放行），生产环境等于敞开跨域。
改法：用环境变量 `CORS_ORIGIN` 指定允许的来源，未配置时默认放行 localhost（开发），生产由 pm2/ecosystem 注入正式域名。
```js
const origin = process.env.CORS_ORIGIN
if (origin) res.setHeader('Access-Control-Allow-Origin', origin)
else res.setHeader('Access-Control-Allow-Origin', req.headers.origin || '*')
```
同源部署（nginx 反代 /api）其实不跨域，CORS 主要是给「前端域名 ≠ 后端域名」的备选方案兜底。

**1.2 登录限流（防暴力破解）**
现状：`POST /api/auth/login` 无任何频率限制，可无限尝试密码。
改法：引入 `express-rate-limit`，对 `/auth/login` 按 IP 限 5 次/分钟；连续失败可叠加短时封禁。这是《服务器部署整改方案.md》第五节的遗留 TODO，建议本轮落地。

**1.3 全局安全响应头（helmet）**
现状：未设置 `X-Content-Type-Options`、`X-Frame-Options`、`Strict-Transport-Security` 等安全头。
改法：引入 `helmet` 中间件（一行接入），HTTPS 部署后顺带开启 HSTS。

**1.4 express.json 限制调回小值**
现状：`server/src/index.js` 写 `express.json({ limit: '100mb' })`，注释说上线第 2 步后调回——现在 OSS 已就绪，该调了。
改法：改为 `express.json({ limit: '10mb' })`。理由：后端模式小文件（≤1.5MB）以 dataUrl 内嵌，base64 后约 2MB；其余大文件全部走 OSS 直传，JSON 不会再承载百兆数据。调小后能明显降低内存与慢请求风险。

**1.5 备份方式修正（WAL 一致性，重要）**
现状：文档第 8 步用 `cp muye.db` 备份。数据库开了 `journal_mode = WAL`，运行期间直接拷 .db 可能漏掉 WAL 里未合并的事务。
改法（二选一）：
- 推荐：备份脚本里用 `sqlite3 /var/www/muye/server/data/muye.db ".backup '/var/backups/muye/muye-$(date +%F).db'"`（sqlite3 CLI 的在线安全备份）；
- 或：`VACUUM INTO '/var/backups/muye/muye-$(date +%F).db'`（SQLite 内置在线导出，无需停服）。
并在恢复演练里验证一次「用备份文件能正常启动」。

**1.6 OSS 密钥最小权限 + 不落库**
现状：代码用 `OSS_ACCESS_KEY_ID/SECRET` 直连，文档让填主账号 AccessKey。
改法：在云控制台创建「RAM 子账号」，只授该私有桶 `PutObject / CompleteMultipartUpload / ListParts / GetObject` 权限，策略限定前缀 `uploads/*`；主密钥不要用于生产。密钥只放在 pm2 `ecosystem.config.js` / 服务器环境变量里，仓库提供 `.env.example` 模板但不提交真实值。

**1.7 未配置 OSS 时返回明确状态码**
现状：`/files/upload-token` 未配置时抛 `expose` 错误返回 400，前端捕获后回退旧逻辑，功能安全。建议把 400 改成 503（服务不可用）并保留中文提示，语义更准确，同时前端仍按「非 2xx」统一回退，无破坏。

### P1 · 上线后尽快（稳定性 / 成本 / 性能）

**1.8 上传与订单绑定 + 孤儿文件回收（重点）**
现状：前端「选文件时就上传」（`readOrderFile` 内调 `uploadOrderFile`），但上传发生在订单创建之前；如果用户选了文件又取消下单，这个 OSS 对象就成了「孤儿文件」，永久占存储、持续计费。分片 init 也一样。
改法（三层）：
1. 后端加一张 `uploads` 表（key / uploadId / 账号 / 状态 / 创建时间），`upload-init` 与 `upload-token` 时写入，`upload-complete` 标记完成；
2. `upload-part-url` / `upload-complete` 校验 key+uploadId 是否属于当前登录账号（防 A 用户补完 B 用户的分片 / 防任意 key 签名）；
3. 两个兜底：① 定时任务清理「创建超过 N 小时仍未 complete / 未关联订单」的上传记录并删除 OSS 对象；② OSS 桶配置生命周期规则，对 `uploads/*` 超过 7 天未完成的分片自动中止、对孤儿对象可先手动巡检。

**1.9 bootstrap 按角色在 SQL 层过滤 + 先过滤再签名**
现状：`routes.js` 的 `/bootstrap` 先把全部订单/流水/消息拉出来 `.map(maybeSignOrder)` 再在 JS 里按角色 filter。医院角色也会给「所有订单」生成签名 URL（不返回但浪费），且全表读取随数据量线性变慢。
改法：
- 医院角色直接 `WHERE client_id = ?`、消息/流水同理，在 SQL 层裁剪；
- 先做角色过滤，再对「最终要返回的订单」调用 `maybeSignOrder`，减少签名计算；
- 设计师的 `designParams` 目前返回全部医院参数，建议只返回「其可见订单对应客户」的参数（隐私收口）。

**1.10 增量同步替代每 15 秒全量**
现状：`App.tsx` 每 15 秒 `refreshFromServer()` 拉一次全量 `/bootstrap`。数据量小时没问题，订单/流水上千后响应体会越来越大。
改法：新增 `GET /api/sync?since=<时间戳>` 只返回该时间点之后变化的数据（orders / notices / txns），前端合并；或改 SSE 推送。这是中长期优化，先记录不急着做。

**1.11 会话过期清理**
现状：`sessions` 表只增不删，过期令牌要等被用到才清理。
改法：启动时 + 每日定时执行 `DELETE FROM sessions WHERE expires_at < now()`，防止表无限膨胀。

**1.12 数据库小加固**
- `PRAGMA busy_timeout = 5000`（并发写等待，避免 WAL 下偶发 SQLITE_BUSY）；
- 索引已有 `orders(client_id / designer_id / status)`、`point_txns(client_id)` 很好，建议补 `notices(client_id, is_read)`、`orders(client_id, created_at)` 复合索引、`sessions(token)`（已主键即索引，确认即可）；
- 引入 `schema_version` 迁移机制：`meta` 表记录当前 schema 版本，改动表结构走「版本号 + 增量迁移」而不是直接改 `schema.sql`，为将来加列（如订单里存 OSS key 归属）留安全通道。

**1.13 下载签名过期处理**
现状：bootstrap 返回的下载 `url` 有效期 2 小时；用户把页面挂 2 小时后点下载会 403。
改法：新增 `POST /api/files/download-url { key }`（校验该 key 属于当前可见订单），前端「点击下载时」实时要签名，不再依赖缓存的旧 url；bootstrap 里也就不用预先批量签名了（与 1.9 呼应）。

**1.14 请求日志与审计**
现状：只有 `console.error` 打异常，没有访问日志，出问题难回溯。
改法：加简单请求日志中间件（方法、路径、状态、耗时、账号/角色，脱敏），或引入 `morgan`；写操作建议落一条结构化日志，便于审计积分/订单变更。

### P2 · 可维护性（有空再做）

- 拆分 `routes.js`（32KB 单文件）：`auth.routes.js` / `orders.routes.js` / `admin.routes.js` / `files.routes.js`；
- 请求校验上 `zod`（前端已装 zod，后端共享同一套 schema，前后端校验规则不跑偏）；
- 上传接口限流：每账号每分钟 `upload-init` 次数上限（防恶意刷 OSS）；
- 可选：文件扩展名白名单（stl/ply/obj/jpg/png/zip…）与「文件名清洗」（存订单时用服务器生成的 key，不受前端文件名影响，现状已如此）；
- `engines` 锁定 Node 版本 + 仓库加 `.nvmrc`；
- 给 `/api/health` 增加 DB 与 OSS 探活字段（`db:ok, oss:configured`），方便监控。

---

---

## 二、前端优化建议

### P0 · 上线前必须做

**2.1 生产构建固定 VITE_API_BASE=/api**
现状：`website/src/lib/api.ts` 默认 `http://<hostname>:3001/api`（明文 http + 硬编码端口）。如果生产环境忘了配 `VITE_API_BASE=/api`，走 HTTPS 域名时会出现「混合内容（Mixed Content）」被浏览器拦截，或端口/跨域全错。
改法：提交一个 `website/.env.production` 内容固定为 `VITE_API_BASE=/api`，这样服务器上直接 `npm run build` 就对，杜绝遗漏（部署文档已写，落地为文件更保险）。

**2.2 上传进度条 UI（体验刚需）**
现状：`uploadOrderFile(file, onProgress)` 已预留进度回调，但界面还没接入；600MB 文件上传时用户看不到任何进度，会误以为卡死。
改法：在医院端「选文件」与设计师端「提交设计」两处接进度展示——多文件逐个显示百分比/已传大小，失败项标红可重试。这是大文件方案闭环里最该补的一环。

### P1 · 稳定与体验

**2.3 apiFetch 加超时与取消**
现状：`apiFetch` 没有 AbortController，后端挂起时前端请求会一直 pending，页面停留在加载态。
改法：给 `apiFetch` 加 30 秒超时（可对上传类单独放宽），超时抛错走统一错误提示；组件卸载时可 abort 未完成的查询。

**2.4 上传时机与孤儿文件提示**
现状：`readOrderFile` 在选文件瞬间就上传到 OSS（配合后端 1.8 回收）。前端侧建议：
- 体验层面：上传成功但未下单前，在表单里显示「已暂存 N 个文件」；
- 若后端回收机制未上线，前端至少在上传失败/取消时主动提示「文件已上传，取消订单后请勿重复上传」；
- 设计上更稳妥的是「提交订单那一刻才真正上传」（表单校验通过再传），可降低孤儿文件与重复上传概率，但会加长「提交」的等待，需权衡。

**2.5 分片续传健壮性**
现状：断点续传依赖 localStorage 里的 `uploadId`；若超过 OSS 的分片生命周期，`completeMultipartUpload` 会报错，目前没有「重新 init 重传」的兜底。
改法：在 `uploadMultipart` 捕获「uploadId 失效」类错误时清检查点、重新 `apiUploadInit` 重传；单个分片 PUT 失败重试 2~3 次（指数退避）再整体失败。

**2.6 大文件前端预校验**
现状：前端只在读文件时对大小无显式拦截（1GB 上限靠后端报错）。
改法：选择文件时就校验 `file.size <= 1GB`、对明显非口扫/照片的类型（可选）给提示，减少浪费在「传了一半被拦」的流量。

**2.7 下载链接实时签名（配合后端 1.13）**
现状：下载依赖 bootstrap 里的旧签名，页面挂久了会 403。
改法：点击下载/预览时调用 `apiGetDownloadUrl` 拿新签名再打开，链接永不过期。

### P2 · 质量与可维护

**2.8 组件级测试（质量清单 P2 未完成项）**
现状：核心纯函数与后端同步层测试很扎实（store.ts 99%+），但三端页面交互还没有组件测试。
改法：按《前端改造与质量清单.md》P2 清单落地 Vitest + React Testing Library + jsdom，覆盖：医院端下单校验/积分不足/撤回/返工、设计师端接单/提交/退回、管理端账号/积分/分组，以及「上传进度与失败重试」的 mock 测试。

**2.9 轮询改增量 / 长连接（配合后端 1.10）**
现状：15 秒全量 bootstrap 轮询。
改法：接增量 `sync` 接口或 SSE；至少先把 `refreshFromServer` 在后台运行时避免打断用户正在编辑的本地状态（当前已靠乐观更新 + 覆盖刷新，注意输入框焦点场景）。

**2.10 store.ts 拆分（50KB 单文件）**
现状：`website/src/lib/store.ts` 超过 50KB，单文件承载全部业务。
改法：按「订单 / 积分流水 / 分组匹配 / 消息 / 账号」拆成模块（保留统一导出，外部 API 不变），降低后续维护与测试成本。

**2.11 大数据量分页**
现状：管理端订单/流水一次性渲染全部。
改法：订单/消息列表接口加分页参数（`page/pageSize`），前端滚动加载；当前量级不着急，量上来前做。

---

## 三、部署与运维建议（配合文档补强）

| # | 建议 | 说明 |
|---|---|---|
| 1 | nginx 开启 gzip/brotli + 静态资源缓存 | `website/dist/assets/*` 文件名带 hash，可设 `Cache-Control: max-age=31536000, immutable`；html 不缓存。减少重复下载 |
| 2 | pm2 用 `ecosystem.config.cjs` | 集中管理 OSS 密钥、`max_memory_restart`、日志路径；避免每次手敲环境变量 |
| 3 | 健康检查增强 | `/api/health` 返回 db / oss 状态，监控脚本定期 curl 并告警 |
| 4 | OSS 账单预警 | 设置云厂商账单阈值告警（文档 12.10 已提，落实成告警规则） |
| 5 | 备份异地 + 恢复演练 | 每日备份到 OSS 另一桶/另一台机器，每月做一次恢复演练（1.5 的方法验证） |
| 6 | 演示账号处置 | 上线前删掉或改密所有种子账号（admin / mingzhou / hengmei / yahe / li…），并清空种子数据中的示例订单 |
| 7 | 带宽说明（给小白） | 文件上传/下载都直连 OSS，不走服务器 3M 带宽；3M 只承载网页与接口，600MB 文件不会被服务器带宽卡住。OSS 下行流量单独计费，走 CDN 更便宜 |

---

## 四、建议的下一轮执行顺序

**第一轮（上线阻断，做完即可发布）**
后端：CORS 白名单（1.1）→ 登录限流（1.2）→ helmet（1.3）→ json 限制 10mb（1.4）→ 备份方法修正（1.5）→ OSS 子账号密钥（1.6）
前端：`.env.production`（2.1）→ 上传进度条（2.2）→ apiFetch 超时（2.3）
文档：修正〇章 6 处滞后（测试数量、分片接口、nginx body 限制、服务器规格定稿、版本号）

**第二轮（上线后 1-2 周）**
上传与订单绑定 + 孤儿回收（1.8）→ bootstrap 角色 SQL 过滤与先过滤后签名（1.9）→ 会话清理（1.11）→ 下载实时签名（1.13 + 2.7）→ 分片续传兜底（2.5）→ 请求日志（1.14）→ DB busy_timeout 与复合索引（1.12）

**第三轮（有空再排）**
增量同步（1.10 + 2.9）→ routes.js / store.ts 拆分（2.10）→ zod 校验（P2）→ 组件级测试（2.8）→ 分页（2.11）→ 健康检查增强（运维 3）

---

## 五、本轮是否要改代码？

本文是「优化建议清单」，按你的节奏执行：
- 如果你想先上线，把「第一轮」做完就够；
- 想趁现在一次改到位，我按「第一轮 + 第二轮」一起改并补测试，CI 全绿后再 push；
- 任何一轮都保持「改完跑一遍 `npm test` / `npm run test:coverage` / `npm run build` / `npm run lint`」的红线不变。
